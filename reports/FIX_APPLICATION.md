---
generated-by: Codex G1 owner
date: 2026-07-14
inputs:
  - AGENTS.md
  - MONKEYBEE_CAMPAIGN_CHARTER_v1.md
  - CYCLE_0_WORK_ORDER.md
  - AUDIT_FINDINGS_LEDGER.md
  - MONKEYBEE_PDF_PLAN_REVISION_7_ALIEN_AUDIT.md
  - DISPUTES.md
  - decisions/D1_CONTENT_HASH.md
  - decisions/D2_CANONICAL_SERIALIZATION.md
  - decisions/D3_DECODED_STREAM_IDENTITY.md
  - decisions/D4_EVIDENCE_CHECKER_ISOLATION.md
  - decisions/D5_PROJECT_LICENSE.md
  - decisions/D6_LEDGER_BACKEND.md
  - decisions/D7_RUST_TOOLCHAIN_PIN.md
  - decisions/DECISION_INDEX.md
  - decisions/DEFERRED_REGISTER.md
  - reports/INGEST_REPORT.md
  - "gauntlet/ROUND_LOG.md (bounded R13 lines 1497-1577 only)"
  - "gauntlet/ROUND_LOG.md (bounded R14 lines 1815-2081 only)"
  - constitution/MONKEYBEE_CONSTITUTION_v8.md
  - constitution/SPECCARD_PIPELINE.md
  - shell/MONKEYBEE_SHELL_CORPUS_v8.md
status: PROPOSED
evidence-status: provisional-pending-substrate
---

# G1 Fix Application and Extraction Map

## 1. Scope and result

G1 extracted the Revision 7 kernel into `constitution/MONKEYBEE_CONSTITUTION_v8.md`, retained the non-kernel planning surface in `shell/MONKEYBEE_SHELL_CORPUS_v8.md`, and created an empty SpecCard pipeline in `constitution/SPECCARD_PIPELINE.md`. Canonical inputs were not edited.

The Audit Findings Ledger has 17 rows carrying an `OPEN-C0` treatment: nine R1 rows and eight R2 rows. Fifteen have an available source and are applied in new G1 outputs. Two, R2-N7 and R2-N8, remain `SOURCE-UNAVAILABLE` under D-002. R1-10 is separately marked `RESOLVED via v8 split` and is mapped in §4. No related document or model recollection was substituted.

## 2. Extraction boundary and normative homes

| Law or corpus | One home in v8 | Shell treatment |
|---|---|---|
| Clean-room and SpecCard law, Revision 7 §4 | Constitution §4 | Inline kernel stub plus document-wide `generated-echo` scope |
| Non-negotiable principles, §6 | Constitution §6 | Inline kernel stub plus `generated-echo` scope |
| Layer and dependency direction, §8–8.1 | Constitution §8–8.1 | §8.2 remains a provisional shell heuristic |
| Identity, evidence, work, and determinism law, §§9–11 | Constitution §§9–11.10 | Shell applications are generated echoes and cannot amend the kernel |
| Crate-boundary budget, §30.1.1 | Constitution §30.1.1 | Concrete §30 atlas remains provisional; the removed subsection is an inline generated echo |
| Evaluation and refinement law, §§33–34 | Constitution §§33–34.9 | Inline kernel stub; Charter owns cycle staging |
| Domain/workflow/bet/repository/atlas/risk/open-decision corpus | Shell stable sections | Provisional reference for archaeology-first delta plans |
| Appendix B examples | Shell Appendix B, generated from Constitution §§10.6, 10.8, and 10.10 obligations | Non-normative `generated-echo`; family, common-envelope, and validator laws remain in the Constitution |

Concrete hash, digest-width, and canonical-serialization bindings remain proposed pending human ratification. D1 proposes SHA-256 at 256 bits and couples it to D2; G1 selected neither D2's deterministic-CBOR proposal nor its alternatives. Constitution §9.2.1 preserves Rev 7's required properties, blocks production identity minting while the binding is pending, and records that hash/grammar ratification must precede the first external commitment. D3's recipe-identity/post-commit-digest split also remains proposed. D-004 keeps `DecodedContainerId` as the sole domain-canon name; `DecodedStreamId` appears only to identify campaign-layer naming drift and is not an exposed alias or second identity. D-005 keeps dependency-policy and cryptographic-crate choices outside G1's authority and pending human routing or ratification, notwithstanding D5's unratified allowlist proposal.

## 3. OPEN-C0 application register

| Ledger ID | Disposition | Exact new-output location | Application |
|---|---|---|---|
| R1-1 | `APPLIED` | Constitution §10.10; shell Appendix B introduction and B.1–B.5 | Added the schema-example generation/validation law. Regenerated examples bind the mandated source/root/graph/view/derivation identities and the §10.8 common semantic-report/run-observation fields; B.3 role-labels every document input and supplies a distinct `ExpectedStateId` for each. |
| R1-2 | `APPLIED` | Shell §7, Bet 24 | Replaced declarative proof-kernel targeting with three candidate invariants pending the scope decision. |
| R1-3 | `APPLIED` | Shell §31.4, required behavior | Defined two forensic paths: one independently authored tool and one second independently sourced path; both disclose lineage and correlated/unknown lineage is not counted twice. |
| R1-4 | `APPLIED` | Shell §22.4, Canonical mode | Byte identity now requires a serialization-total `SemanticScopeId` for the selected profile and names what totality must enumerate. |
| R1-5 | `APPLIED` | Shell §35.1 | Added the human independence-ecology assumption, role/cost planning, and honest downgrade path. Charter day-zero/degradation treatment remains higher campaign authority. |
| R1-6 | `APPLIED` | Constitution §11.10; shell §29.11 generated echo | Added intake, triage, containment, embargo/coordination, advisory schema, claim-lapse linkage, and closure law. Concrete channels remain `PENDING-HUMAN-ADOPTION`; no external action occurred. |
| R1-7 | `APPLIED` | Shell §13.1, §16.5, §16.8, §21.1, §21.7.1, §23.12, and §24.2; SpecCard slots `MB-SC-R0-038`–`040` | Added encrypted-payload/unencrypted-wrapper inventory; `PhoneticAlphabet`/`Phoneme`; `Prop_Build`; PDF 2.0 UTF-8 text-string inventory; PDF/VT profile naming; `/Tabs`; and `NeedsRendering`. All standards semantics remain pending licensed source. |
| R1-8 | `APPLIED` | Constitution §11.2 and §11.2.3; shell §22.3 and §32 risk row | Added a canonicalization/symmetry limit dimension, graph-isomorphism-class cost statement, charged work counters, typed exhaustion behavior, and the adversarial-symmetry DoS risk. |
| R1-9 | `APPLIED-PROPOSED-BINDING` | Constitution §10.6.1; shell Appendix A.13 generated echo | Added five proposed axes: code isolation; different-model-family authorship; specification-only inputs excluding producer source/rationale/fixtures; per-cycle seeded-defect detection measurement; and lineage/correlation plus red-team/human review. Producer parser/tree/encoder/root logic is prohibited from sharing; only narrow frozen constants, audited primitives, and independently authored vectors may cross. Human ratification and observed detection evidence are still required. |
| R2-N1 | `APPLIED` | Shell §3.0 R0 and R2 rows | R0 now requires structural signature/`ByteRange` discovery. R2 now requires structural signature-impact classification including `DocMDP`, `FieldMDP`, and locks while excluding cryptographic-trust conclusions. |
| R2-N2 | `APPLIED` | Constitution §11.2.3; shell §26.7 generated echo | Required DoS-resistant keyed hashing or ordered structures for attacker-influenced keys. A hasher swap requires a consequence-contract threat/complexity/evidence/rollback note. |
| R2-N3 | `APPLIED` | Constitution §11.2 and §11.2.3; shell §22.3 and §32 | Added the missing work dimension and adversarial-symmetry risk/fallback. |
| R2-N4 | `APPLIED` | Constitution §10.4 and §11.1; shell Appendix A.2 | `ChildContextStorage` retains the computed parent/local `Limits` intersection. The collector is `Sync` with bounded interior mutability and deterministic worker-batch merge; non-re-entrancy is scoped to callbacks/operation re-entry. |
| R2-N5 | `APPLIED` | Constitution §33.6 | Distinguished repository-level Baselines C/D from mechanism-level ablations and prohibited one run from being counted as both evidence classes. |
| R2-N6 | `APPLIED` | Shell §21.1 and §21.7.1 | Added `/Tabs` and catalog `NeedsRendering` to the explicit inventory. |
| R2-N7 | `SOURCE-UNAVAILABLE` | D-002; shell use warning; this report §5 | `ALIEN_ARTIFACT.md` is absent. No successor or reconstructed stamp was invented. If restored, hash/read the exact file, then add an explicit superseded-in-part notice or authorized successor without altering unseen content. |
| R2-N8 | `SOURCE-UNAVAILABLE` | D-002; shell use warning; this report §5 | `PROJECT_OVERVIEW.md` is absent. No filter-list or browser-coupling prose was reconstructed. If restored, hash/read the exact file, then apply the authorized non-exhaustive-list and wording treatment in a successor or permitted edit. |

## 4. Remaining Round 1 and Round 2 rows

| Ledger ID | Status in ledger | G1 mapping |
|---|---|---|
| R1-10 | `RESOLVED via v8 split` | Constitution extraction contract and shell generated-echo law create one normative kernel home; shell applications cannot modify it. |
| R1-11 | `NOTED` | No normative change. G1 preserved the source feature surface and did not manufacture a fix for cosmetic or context-dependent wording. |

## 5. Source-unavailable carry-forward

The morning ratification queue must carry both unavailable-source rows:

- R2-N7 depends on the exact `ALIEN_ARTIFACT.md` bytes.
- R2-N8 depends on the exact `PROJECT_OVERVIEW.md` bytes.

Restoration procedure: place the exact artifact in the checkout; record its identity in the canonical baseline; read it under the authority hierarchy; reproduce or dispute the ledger evidence; then apply the authorized treatment in a new output or expressly permitted successor. Until that happens, both rows remain unresolved. A related file is context, not a substitute.

## 6. Campaign-layer rows C-1 through C-12

These rows are already `RESOLVED-CHARTER`. G1 maps them to preserved or extracted support without claiming a second resolution.

| ID | Charter disposition preserved by G1 | G1 location or no-change rationale |
|---|---|---|
| C-1 | Credentialed standard-handler read decryption is C1; public-key support is later | Shell §3.0 remains compatible; SpecCard slots `MB-SC-R0-023`–`031` cover pending R0 semantics. |
| C-2 | SpecCard pipeline is day-zero and per-cycle gated | `constitution/SPECCARD_PIPELINE.md` contains registry, R0 map, extraction protocol, and review template only. |
| C-3 | CLI/WASM/C ABI are staged explicitly | Shell §25 preserves all surfaces; Constitution §10.10 keeps surface semantics tied to consequence contracts. |
| C-4 | Performance has kernel, tracking, and hardening stages | Constitution §11.9 preserves determinism classes; shell §26 remains provisional program content. No measurement ran. |
| C-5 | Structured authoring lands in the Charter's C6 track | Shell §22 preserves the authoring/layout surface. G1 made no scheduling change. |
| C-6 | Corpus sealing and release engineering remain staged | Shell §28 preserves corpus law; Charter remains the schedule authority. |
| C-7 | Layer law/budget are kernel; concrete atlas is provisional | Constitution §8 and §30.1.1; shell §30 without a duplicate budget law. |
| C-8 | Clause tagging begins at C1; C6 builds the scorer | Constitution §10.10 preserves contract propagation; G1 adds no retro-tagging claim. |
| C-9 | Git alone is not the commitment substrate | Constitution §33.4 preserves tamper-evident precommitment; the extraction remains provisional. |
| C-10 | Flagship F and close probes are distinct; G/H remain named | Shell §31 preserves the flagship definitions; Constitution §34.9 preserves the close probe. |
| C-11 | Campaign timing ends at day 15 | Charter-only schedule correction; no G1 duplication. |
| C-12 | Baselines A–D and E where feasible remain visible | Constitution §33.5 preserves all five baseline classes and their scoped evidence role. |

## 7. Strategic rows S-1 through S-4

These rows are `RESOLVED-REASONING`. G1 does not reopen them.

| ID | Preserved treatment |
|---|---|
| S-1 | Constitution §33 retains separate artifact, foundry, and lineage ladders; no construction-era provenance deficit is generalized into an irrecoverable artifact-assurance claim. |
| S-2 | Timeline/end-point reconciliation remains in campaign reasoning and Charter; no shell duration claim supersedes it. |
| S-3 | The Charter's graceful-degradation law remains campaign authority; G1 evidence machinery does not gate the survival wedge. |
| S-4 | Shell §25.3 preserves the actual CLI surface without inventing a command name; no new percentage or comparative measurement was generated. |

## 8. SpecCard contamination check

- Registry slots: 43.
- Slot state: 43 of 43 `PENDING-LICENSED-SOURCE`.
- Semantic bodies authored: 0.
- Licensed or normative source fetches by G1: 0.
- Prohibited processor source/documentation contacts by G1: 0.
- External actions by G1: 0.

## 9. Rule 13 vocabulary disposition

The mandatory pre-SELF-CHECK scan used the case-insensitive vocabulary `best|complete|fastest|mogged|supremacy` over `constitution/**` and `shell/**`.

| Snapshot | Matching lines | Matching occurrences | Disposition |
|---|---:|---:|---|
| Root's early bounded scan, reported by the human | 167 | Not supplied | Trigger for the mandatory manual gate; taken before the G1 draft stabilized. |
| G1 first scan after full extraction | 203 | 252 (`best` 26; `complete` 154; `fastest` 0; `mogged` 1; `supremacy` 71) | Every line was inspected; ordinary copied uses were rewritten without removing their underlying requirement. |
| Final pre-SELF-CHECK scan | 160 | 202 (`best` 24; `complete` 113; `fastest` 0; `mogged` 1; `supremacy` 64) | 42 Constitution lines and 118 shell lines retained only in the scoped structures below. |

The rewrite removed all matching vocabulary from 43 ordinary hit-bearing lines and removed 50 matching occurrences. It changed descriptors, not law: examples include replacing generic whole/finished wording with exact source, envelope, workflow, or lifecycle terms, and replacing “not a complete spec” with “not a standalone specification.” No bulk deletion or global substitution was used.

Every one of the 160 retained lines was manually dispositioned into at least one of these permitted structures:

- typed operation, evidence, coverage, source, transaction, or report outcomes, including literal `Complete`/`Partial` variants and explicit truncation/availability contrasts;
- machine-scoped release, capability, profile, constraint, or serialization envelopes whose denominator and refusal behavior are stated at the same locus;
- comparison-claim scope, lifecycle, field definition, denominator, outcome, expiry, risk, falsifier, or withdrawal records under Constitution §10.3 and shell §24;
- foundry trial-distribution, baseline, and ablation protocols, including the exact supplied §34.9 sealed-record field “median and best outcomes”; or
- explicit Rule 13 anti-overclaim examples, where `best` or `mogged` is named only to prohibit replacing typed evidence with that label.

No retained line is ordinary promotional prose. The retained occurrences do not assert that MonkeyBee presently satisfies a comparative, release-envelope, or field-lead claim.

## 10. C0/R13 SELF-CHECK evidence (historical checkpoint)

This section records the checkpoint before R14. Section 15 is the current R14 successor; counts here, including the then-current 40-slot registry, are preserved as historical evidence rather than silently rewritten.

| Check | Result |
|---|---|
| Canonical integrity | All nine canonical inputs match `ledger/CANONICAL_HASHES.md` byte-for-byte under SHA-256. |
| Scope and artifact type | Branch is `main`. G1 produced five owned Markdown artifacts: two under `constitution/`, one under `shell/`, this report, and `ledger/owners/G1_STATE.md`. No code, bead, branch, worktree, commit, push, or external action was created. Concurrent files outside G1 scope were not edited. |
| Heading and feature preservation | Rev 7 has 554 unique level-2–4 headings; the Constitution/shell union contains every one, with zero exact-heading omissions. Of 5,266 unique nonblank source lines, 142 lack a byte-identical output line: the prior 139 accounted lines plus three R13 replacements—the two original `DivergenceReport` identity bullets and the prior single-paragraph §10.8 identity statement. The replacement text preserves and closes those requirements; none of the 142 is an unaccounted feature loss. |
| Normative homes | Sentinel text from §9, §11, §30.1.1, §33, and §34.9 occurs once in the Constitution/shell union. Shell kernel locations are short stubs plus explicit `generated-echo` citations; the document-wide echo rule prevents shell applications from amending the Constitution. |
| Supplied §34.9 | A normalized nonblank-text diff against Work Order §3 passes exactly, including the sealed-cycle-record closing sentence. |
| Audit mapping | Every R1, R2, C, and S identifier in `AUDIT_FINDINGS_LEDGER.md` appears in this report. The 17 `OPEN-C0` rows resolve to 14 `APPLIED`, one `APPLIED-PROPOSED-BINDING`, and two `SOURCE-UNAVAILABLE`. |
| SpecCard boundary | Registry IDs are the exact sequential set `MB-SC-R0-001`–`040`; every registry row carries three `PENDING-LICENSED-SOURCE` fields; semantic bodies authored: zero. |
| Appendix B | All five fenced JSON examples parse with `jq`. Assertions preserve every prior §10.6 family field represented by B.1–B.4 and every Appendix A.13 checker field represented by B.5, including role-labeled `ExpectedStateId` bindings. All five also pass the §10.8 common-field/backlink/exclusion assertions; B.2 retains both telemetry values; B.4 passes separate version and processor-output binding checks plus exact order-independent output-set projection. |
| Markdown/provenance hygiene | All five files have the required provenance fields, balanced fences, UTF-8 text, zero trailing-whitespace hits, and zero conflict markers. |
| Cross-owner reconciliation | D-004/D-005 and D1–D7 plus the index/deferred register were reread after G2 submission. `DecodedContainerId` is the sole canonical name; D1/D2/D3/D4 remain proposed; dependencies and cryptographic crates remain pending; hash/grammar ratification precedes the first external commitment. |
| Rule 13 | The final mandatory result remains 160 manually dispositioned lines / 202 occurrences, with the exact per-term counts in §9. The only additional hits in the report/state are inside this enforcement record. |
| Clean room and phase limits | No URL occurs in G1 outputs. Processor names occur only in extracted canonical prohibitions or dated scoped-claim structures; the R13 added-line diff contains zero prohibited processor names and zero URLs. G1 contacted no processor source/documentation, fetched no source, ran no comparison or product measurement, authored no SpecCard semantics, and performed no external action. |
| R13 targeted regressions | A01 typed-identity/linkage checks pass; A02 common fields occur for B.1-B.5 with no friendly semantic-report alias; A03 finds two separate processor versions; B01 finds two in-record output bindings whose set equals the retained summary; B02 preserves exactly three `SourceIdentity` variants, `len: Option<u64>`, range/whole-source limits, revocation, and collision quarantine. Both B findings remain Grade B in the fix map. |

SELF-CHECK found and repaired four draft defects: an unowned project-TLV grammar and post-decode-only decoded identity were replaced by the D1/D2/D3 ratification gate; R1-9 was expanded from code isolation to the five proposed premise-isolation/measurement axes; the report's erroneous 18-row `OPEN-C0` count was corrected to 17 and R1-10 moved to its canonical resolved status; and inherited §35 dependency/crypto defaults were made explicitly pending under D-005.

Three verifier false starts are disclosed: `ruby` was unavailable, so the JSON check was rerun successfully with `jq`; the first SpecCard sequence comparator formatted expected IDs with two digits, then passed with three; and the first §34.9 range comparator dropped the EOF closing sentence from its comparison, then passed with the correct EOF range. None changed evidence or content silently.

R13 SELF-CHECK added three disclosed diagnostic corrections: the first multi-check wrapper failed JavaScript parsing before any shell check ran and was replaced by smaller commands; the first provenance-correction patch used the wrong local bullet order, changed no file, and was reapplied against exact anchors; and one aggregate diff display truncated, after which the changed artifacts were inspected through bounded per-file diffs. These were diagnostic false starts, not evidence or semantic failures.

## 11. R13 identity-law revision application

R13 was a filed, mechanically validated, terminated identity-law-consistency packet over the exact four-file pre-task manifest recorded in `ledger/owners/G1_STATE.md`. Its grades were intake metadata, not proof. The replacement owner tested every premise against the local authority chain before editing. The filed counts remain `A=3`, `B=2`, `C=0`; neither Grade B finding was promoted. The filed-time correction and bounded Audit Ledger exposure did not validate or regrade a finding.

| Finding | Premise disposition | Smallest applied repair | Loss-guard result |
|---|---|---|---|
| `R13-A01` (`A`) | `QUALIFIES` | Constitution §9.2 now types the semantic-report and run-observation protocol/content identities. §10.8 closes their value domains and one-way linkage while leaving concrete encoding/hash/digest width behind the D1/D2 ratification gate. | Semantic body remains separate from run observations; telemetry stays outside D0; determinism limits and D1/D2 pending status remain explicit. |
| `R13-A02` (`A`) | `QUALIFIES` | Constitution §10.10 now generates and validates the common §10.8 fields. Shell Appendix B B.1-B.5 now carry the common typed placeholders, capability identities, separately versioned observation schema, backlink, coverage/telemetry, and exclusion fact. | Every prior family field remains; B.2 retains its wall-time and peak-memory observations plus explicit telemetry exclusion; examples remain non-normative with pending-ratification markers. |
| `R13-A03` (`A`) | `QUALIFIES` | Shell B.4 adds a separate processor `version` in each processor-result record; Constitution §10.10 makes that a validator requirement. | Processor artifact, version, configuration, environment, profiles, proper-domain identities, output identities, and witness evidence remain separate facts. |
| `R13-B01` (`B`, not promoted) | `QUALIFIES` | Constitution §10.6 makes processor results an unordered identity-keyed set and binds each output within its processor record. B.4 emits those bindings; the retained output-ID list is only their exact generated projection. | Comparison symmetry is explicit; array order has no role; per-processor facts and independently identified outputs remain intact. |
| `R13-B02` (`B`, not promoted) | `QUALIFIES-IN-NARROWED-FORM` | Constitution §9.1.1 closes the already intended out-of-band `AuthorityId` contract and tuple non-reuse/authentication law; §9.2 adds the typed identity meaning. No fourth source class was added. | All three source classes, `len: None`, range-scoped evidence, whole-source promotion limits, source-change revocation, collision quarantine, and D1/D2 pending status remain intact. |

No R13 premise required a conscious non-repair. The broader B02 uncertainty failed only insofar as the preexisting Constitution already stated that the authority contract is out-of-band; the repair addresses the narrower unstated guarantees and records that narrowing. `constitution/SPECCARD_PIPELINE.md` was not changed by R13. At that submission boundary, R14 had not yet been assigned.

## 12. R14 replacement-owner revision application

R14 supplied 19 allegations (`A=5`, `B=10`, `C=4`) through a reviewer process independently classified `METHOD-NONCOMPLIANT` / `PROCESS-QUARANTINED`. The outside-repository scratch manifest and final punctuation variance neither established nor refuted any premise. The replacement owner independently tested every ID against the local authority chain before editing; the full intake, loss guards, evidence searches, narrowed premises, and transition record are in `ledger/owners/G1_STATE.md`. All B and C rows retain their filed grades.

| Finding | Independent disposition | Applied repair and exact home | Preserved loss guard |
|---|---|---|---|
| `R14-A01` (`A`) | `QUALIFIES` | Constitution §10.6 `OpenReport` now requires the §16.8 reachability/effect inventory, metadata across revisions, hypothesis/view coverage, and provenance; generated B.1 emits those fields. | Existing OpenReport fields, graph structure, alternatives, and §10.9 availability/refusal remain. |
| `R14-A02` (`A`) | `QUALIFIES-IN-NARROWED-FORM` | Constitution §34.9 keeps the supplied Work Order text and adds a versioned dependency on shell §27's full G0–G7/G6.1 taxonomy, §27.1 falsifiers, and §27.4 matrix law. | No tier moved, merged, collapsed, or copied; later taxonomy change is a kernel touch. |
| `R14-A03` (`A`) | `QUALIFIES` | Pipeline slots 041–042 reserve catalog `/Version` and extension-dictionary/level inventory and enter dialect coverage. | Per-revision `EffectiveDialect` distinctions remain; all slot state is `PENDING-LICENSED-SOURCE`. |
| `R14-A04` (`A`) | `QUALIFIES` | Lossless-filter coverage now cites existing slot 029. | Filter and security ownership remain separate; both standard-handler rows still cite 029. |
| `R14-A05` (`A`) | `QUALIFIES` | Sections 13–14 below expose the exact 142-line partition and every 43-line Rule-13 before/after locus. | Every source line has an output locus and change authority; no artifact was rewritten to shrink the set difference. |
| `R14-B01` (`B`, not promoted) | `QUALIFIES` | Constitution §§10.6 and 10.8 now enumerate D1R's semantic-plan/non-secret-receipt replay relation. | No output-byte identity is promised; secure encryption/signing/time/service variability stays under §11.9. |
| `R14-B02` (`B`, not promoted) | `QUALIFIES` | Constitution §§11.2 and 11.8 add the dedicated bounded CMap tenant, `usecmap`/entry limits, and version-identified data-pack capability. | CMap remains separate from Type 4 and general PostScript; rights and cache identity remain explicit. |
| `R14-B03` (`B`, not promoted) | `QUALIFIES` | Constitution §11.2 adds hostile XML/XMP parsing separately from static-XFA projection. | Parse and projection budgets remain separately charged and transactionally published. |
| `R14-B04` (`B`, not promoted) | `QUALIFIES` | Constitution §10.6 `TextReport` adds the four text-view classes, per-span CID/GID/evidence/visibility, order type, and alternatives. | Existing fields remain; an order label confers no correctness or semantic-quality claim. |
| `R14-B05` (`B`, not promoted) | `QUALIFIES` | Constitution §10.6 `RenderReport` and B.2 add revision/hypothesis evidence, effective dialect, resources, warnings/refusals, and non-operator unsupported/policy features. | Existing fields remain; wall time and peak memory stay in the run-observation envelope. |
| `R14-B06` (`B`, not promoted) | `QUALIFIES-IN-NARROWED-FORM` | Constitution §4.4 prefixes the verbatim inherited identifier/status paragraph with a paragraph-scoped `[UNVERIFIED]` notice. | No identifier or core/profile/errata distinction was deleted or reinterpreted. |
| `R14-B07` (`B`, not promoted) | `QUALIFIES` | Constitution §9.2 adds the five already-defined identity classes: `SpecCardId`, `ConsequenceContractId`, `EvaluationProtocolId`, `CompetitorDiscoveryProtocolId`, and `CompetitorDiscoveryReportId`. | Identity domains and the D1/D2 encoding-ratification gate remain unchanged. |
| `R14-B08` (`B`, not promoted) | `QUALIFIES` | Cross-reference/revision coverage now cites slot 031. | This is a dependency edge only; encryption rows and credential/tenant/revision partitioning remain. |
| `R14-B09` (`B`, not promoted) | `QUALIFIES` | Constitution §4.3 and the pipeline template/review path add fixture, contract, and external-profile link fields. | Every empty value is `PENDING-LICENSED-SOURCE`; fixtures/contracts/profiles cannot supply normative meaning. |
| `R14-B10` (`B`, not promoted) | `QUALIFIES` | Section 14 records all 43 source/current obligation pairs and stable output homes. | No artifact wording changed for reconciliation; the table records, rather than regenerates, the prior repair. |
| `R14-C01` (`C`, not promoted) | `QUALIFIES` | Constitution §10.6/§10.10 and B.4 rename the summary to `processor_output_artifact_ids`. | Identity-keyed bindings, exact unordered projection, and no role/priority/outcome/majority meaning remain. |
| `R14-C02` (`C`, not promoted) | `QUALIFIES` | Constitution §11.1 now requires caller-provided storage that outlives the child context without naming the shell-only sketch type. | Owned/borrowed lifetime safety, shared non-resettable accounting, and bounded `Sync` diagnostics remain. |
| `R14-C03` (`C`, not promoted) | `QUALIFIES` | Constitution §10.6 and B.1 add the exact-preservation ledger as independent overlapping interval-facet collections. | `unexplained bytes` remains separate; no exclusive enum replaces overlapping facts. |
| `R14-C04` (`C`, not promoted) | `QUALIFIES-IN-NARROWED-FORM` | Pipeline slot 043 and its separate coverage row reserve ISO/TS encryption/integrity inventory. | Slot 043 is absent from caller-credential decryption; C1 remains RC4/AESV2/AESV3 only. |

No R14 premise failed outright. A02, B06, and C04 were admitted only in their narrower locally supported form; no rejected broader implication entered the artifacts.

## 13. Exact source-line exception accounting

The comparison domain is the canonical Rev 7 file at SHA-256 `df7e21d6ce280979eeb4848866ea975c7c934eebff50801191bf1d1c4c45de1b`. Method: strip only the line terminator; ignore blank lines; retain the first occurrence of each distinct source line; compare for byte-identical membership in the Constitution/shell union. This yields 5,266 unique nonblank source lines and exactly 142 exceptions. Stable section identifiers are primary; local source lines are supporting coordinates. The four disjoint partitions below enumerate every exception: `18 + 43 + 78 + 3 = 142`.

### 13.1 Extraction and locally authorized fix exceptions — 18 lines

| Rev 7 locus (supporting line) | Carried requirement | Exact output locus | Change authority |
|---|---|---|---|
| document title (1) | MonkeyBee Revision Seven plan identity | Constitution title plus shell title/introduction | Work Order §1 Constitution/shell split and required provenance |
| document subtitle (3) | clean-room, memory-safe, multi-view PDF-engine scope | Constitution extraction contract plus shell §§1–3 and retained domain corpus | Work Order §1 split; no domain omission |
| §3.0 R0 row (361) | autopsy envelope, encryption inventory, recovery, reports, and exclusions | shell §3.0 R0 row with structural signature/`ByteRange` discovery added | Work Order §2 / R2-N1 |
| §3.0 R2 row (363) | writer envelope, reopen/interoperability/preservation, and exclusions | shell §3.0 R2 row with structural signature-impact classification added | Work Order §2 / R2-N1 |
| Bet 24 (1108) | proof artifacts, theorem scope, trusted base, and ordinary-evidence boundary | shell Bet 24, with first-scope items classified as candidate invariants | Work Order §2 / R1-2 |
| §10.4 (1811) | bounded diagnostics, observer isolation/failure, deterministic truncation | Constitution §10.4 plus bounded `Sync` collector and deterministic merge law | Work Order §2 / R2-N4 |
| §16.8 (3229) | reachability/effect graph over active-content families | shell §16.8 with encrypted-payload/unencrypted-wrapper inventory added | Work Order §2 / R1-7 |
| §21.7.1 (4260) | separation of stored values, appearances, form actions, XFA, and host results | shell §21.7.1 with catalog `NeedsRendering` inventoried separately | Work Order §2 / R1-7 |
| §22.4 Canonical mode (4429) | semantic-canonical byte identity and provenance-bearing distinction | shell §22.4 with serialization-total `SemanticScopeId` precondition | Work Order §2 / R1-4 |
| §24.2 item 6 (4853) | later archival/engineering/print/accessibility profile sequence | shell §24.2 items 6–7 after explicit PDF/VT insertion | Work Order §2 / R1-7 |
| §31.4 first forensic bullet (6391) | independent forensic attack on redaction output | shell §31.4 one primary independent forensic tool | Work Order §2 / R1-3 |
| §31.4 second forensic bullet (6392) | lineage-diverse second forensic path | shell §31.4 explicitly optional second independent lineage when available | Work Order §2 / R1-3 |
| §35 runtime dependency row (7113) | provisional pure-Rust/permissive/narrow-boundary candidate | shell §35 marks candidate `PENDING-HUMAN-RATIFICATION` | D-005 higher-authority dependency disposition |
| §35 cryptographic crates row (7119) | vetted candidate dependency class and security review | shell §35 marks class `PENDING-HUMAN-RATIFICATION`, selects no crate | D-005 higher-authority dependency disposition |
| §35.1 assumption (7187) | independent corpora/processor-access assumption | shell §35.1 retains it and adds the human-independence ecology/downgrade path | Work Order §2 / R1-5 |
| Appendix A.2 parameter (7257) | local limits input to a child context | shell Appendix A.2 retains `local: &Limits` with caller storage | Work Order §2 / R2-N4 |
| Appendix A.2 return (7259) | child context narrows authority and parent/local limits | shell Appendix A.2 stores parent ∩ local for the returned lifetime | Work Order §2 / R2-N4 |
| Appendix A.13 (7579) | standalone hostile-package checker, availability/coverage, budgets, and dependency boundary | shell Appendix A.13 plus Constitution §10.6.1 five-axis isolation proposal | Work Order R1-9 and D4 proposed binding |

### 13.2 Appendix B schema regeneration — 78 lines

Work Order §1 requires Appendix B to be regenerated from current schemas; Work Order §2/R1-1 requires mandated identities and a schema-example validator. The source examples therefore remain feature inputs, not authoritative serialized shapes. Each row below lists every non-identical source line in that example; the exact output locus is the correspondingly named shell Appendix B example plus the generator/validator law at Constitution §10.10.

| Example | Every Rev 7 exception line | Carried requirement and output locus | Change authority |
|---|---|---|---|
| B.1 Open report | 7589, 7591, 7595, 7596, 7599, 7601, 7602, 7603, 7604, 7606, 7609, 7610, 7613, 7615, 7616, 7618, 7619, 7620, 7622 | Source identity/availability, header evidence, strict/recovery alternatives, revisions/signatures, limits; regenerated at shell B.1 with current §10.6/§10.8 fields. | Work Order §§1–2 / R1-1; R13 A01–A02; R14 A01/C03 |
| B.2 Render report | 7631, 7632, 7633, 7634, 7635, 7636, 7639, 7640, 7641, 7642, 7643, 7644, 7646, 7647, 7648 | Source/page/profiles/surface/artifact, determinism, substitution/refusal, run observations, trace; regenerated at shell B.2 with typed identities and current render fields. | Work Order §§1–2 / R1-1; R13 A01–A02; R14 B05 |
| B.3 Transform receipt | 7656, 7657, 7659, 7660, 7661, 7662, 7664, 7665, 7666, 7667, 7668, 7669, 7670, 7671, 7672, 7673, 7674, 7675, 7678, 7679, 7680 | Policy, role-labeled inputs, roots, operations/deltas, signatures, falsifiers, coverage; regenerated at shell B.3 with `DocumentViewId`/`ExpectedStateId` and common envelope. | Work Order §§1–2 / R1-1; R13 A01–A02 |
| B.4 Divergence report | 7690, 7691, 7693, 7694, 7695, 7697, 7698, 7699, 7700, 7701, 7702, 7704, 7705, 7706, 7707, 7709 | Inputs/processors/profiles, observation/witness, causal subgraph, normative/compatibility analysis; regenerated at shell B.4 with per-processor versions/bindings and exact processor-output projection. | Work Order §§1–2 / R1-1; R13 A01–A03/B01; R14 C01 |
| B.5 Evidence-check report | 7717, 7719, 7720, 7722, 7724, 7726, 7730 | Package/checker identity, unsupported claims, requested/missing coverage, no-claims; regenerated at shell B.5 from Appendix A.13 plus common envelope. | Work Order §§1–2 / R1-1/R1-9; R13 A01–A02 |

### 13.3 R13 replacements — 3 lines

| Rev 7 locus (supporting line) | Carried requirement | Exact output locus | Change authority |
|---|---|---|---|
| §10.6 `DivergenceReport` processor facts (2005) | processors plus exact artifact/version/configuration/environment facts | Constitution §10.6 unordered processor-result record | R13-A03/B01, independently premise-qualified |
| §10.6 `DivergenceReport` identity facts (2006) | exact proper-domain input/output identities and profiles | Constitution §10.6 identity bullet plus exact processor-output set projection | R13-B01, independently premise-qualified |
| §10.8 semantic/run paragraph (2054) | D0 semantic identity, replay class limits, profile/capability binding, telemetry exclusion | Constitution §10.8 closed typed two-body grammar and replay law | R13-A01/A02, independently premise-qualified; R14-B01 adds D1R explicitly |

## 14. Rule-13 before/after obligation register — 43 lines

This is the reproducible interface required by R14-B10 and the Rule-13 partition of R14-A05. Each row names the canonical source line, the exact current stable output home, the wording delta, and the obligation that survived. Change authority for every row is `AGENTS.md` Rule 13 as applied in the original G1 manual scan; it authorized wording repair, never feature deletion. Four other term-bearing source exceptions are intentionally absent: §10.4 line 1811, §22.4 line 4429, Appendix A.13 line 7579, and B.1 line 7599 belong to the OPEN-C0/schema partitions in §13.

| Rev 7 stable locus (line) | Source wording → current wording | Preserved obligation | Exact output home |
|---|---|---|---|
| §0 status (9) | “Competitive Supremacy Doctrine” / “supremacy reports” → “competitive-claim and ambition addendum” / “field-comparison reports” | revision-five-to-seven history and every listed identity/security/evaluation change | shell §0 status |
| §0 status (13) | “complete product” (twice) → “entire product” | broad whole-system legibility before decisions are final | shell §0 status |
| §0.1 (42) | “supremacy doctrine/lanes” → “comparison-claim doctrine/lanes” | dated register, six evidence-gated lanes, bets/workflows/flagships, no execution work | shell §0.1 |
| §0.1 (43) | “Completes” → “Performs” | directed §§10/11/16 merge-interaction pass and all named constraints | shell §0.1 |
| §0.1 (44) | “supremacy program” → “field-comparison program” | anti-cherry-picking controls and lifecycle/outcome separation | shell §0.1 |
| §0.1 (47) | “supremacy reports” → “field-comparison reports” | atomic form execution, bounded XFA, and explicit layer owners | shell §0.1 |
| §0.2 (74) | “Completed” → “Produced” under “has not yet” | clause-by-clause standards matrix remains absent | shell §0.2 |
| §0.2 (75) | “Completed” → “Recorded” under “has not yet” | legal/licensing review remains absent | shell §0.2 |
| §0.3 (94) | “has been completed” → “has occurred” | implementation authorization still requires separate readiness work and no global-redesign blocker | shell §0.3 |
| §2.6 (308) | “first complete release” → “first declared release envelope” | post-release extension absorption frontier | shell §2.6 |
| §5 (672) | “complete user outcomes” → “end-to-end user outcomes” | workflows, not subsystems, organize the plan | shell §5 |
| Workflow Q (920) | “best-effort text/layout” → “a bounded text/layout attempt” | scoped disclosure, hostile-response validation, evidence/conflicts, and replay no-claims | shell §5.17 |
| Bet 13 (1056) | “complete file” → “entire file” | demand-driven page/object queries without eager whole-file materialization | shell Bet 13 |
| §8.2 (1183) | “complete vertical slices” → “end-to-end vertical slices” | vertical-slice advancement rule | shell §8.2 |
| §9.1 (1306) | “complete admitted origin DAG” → “entire admitted origin DAG” | reality commitment composition and separation of transient admission facts | Constitution §9.1 |
| §9.1.1 (1326) | “committed complete length, complete coverage” → “committed final length, whole-source coverage” | durable whole-source exactness threshold and revocation | Constitution §9.1.1 |
| §9.1.1 (1330) | “complete candidate-origin resolution” → “resolution of every candidate origin” | validated `TransformId` constructor invariants | Constitution §9.1.1 |
| §9.6 (1594) | “complete text extraction” → “full text extraction” | lazy views avoid unrelated materialization | Constitution §9.6 |
| §9.6 (1596) | “explicitly complete semantic scope” → “serialization-total semantic scope” | only a scope covering every required semantic axis may key that normalization | Constitution §9.6 |
| §9.6 (1600) | “complete relevant authority snapshot / complete manifest” → “entire relevant authority snapshot / exact manifest needed for the computation” | two-stage reuse requires current authority, dependencies, selection, policy, sensitivity, and isolation match | Constitution §9.6 |
| §9.9 (1643) | “complete provenance” → “end-to-end provenance” | Honeycomb kill metric retains user-visible provenance coverage | Constitution §9.9 |
| §11.8 (2261) | “complete effect journal” → “entire effect journal” | form-action reads/writes/order/resources commit atomically or not at all | Constitution §11.8 |
| §12.1 (2297) | “completed immutable ranges” → “finalized immutable ranges” | blocking host I/O stays isolated and publishes immutable data only after success | shell §12.1 |
| §20.3 (3920) | “complete target” → “declared target” | all enumerated font families remain target scope | shell §20.3 |
| §20.7 (3979) | “complete error traces” → “full error traces” | charstring parsing/evaluation retains error-trace obligation | shell §20.7 |
| §21.4 (4204) | “comparison has been completed” → “comparison has been run” | profile release still requires independent-validator comparison | shell §21.4 |
| §21.7.1 (4267) | “complete effect journal / entire transition” → “entire effect journal / entire transition” | selected graph, host facts, effects, budgets, and atomic rollback remain bound | shell §21.7.1 |
| §22.0 (4345) | “complete positioned semantics / complete mapping” → “positioned semantics are all explicit / exhaustive mapping” | authoring admission, transform derivation, origins, and serialization mapping remain | shell §22.0 |
| §22.9 (4493) | “complete authoring text-layout system” → “general-purpose authoring text-layout system” | PDF does not itself supply that authoring facility | shell §22.9 |
| §24.2 (4851) | “best-supported corpus” → “corpus having the strongest available support” | selected PDF/A profile remains gated on corpus support and independent oracle | shell §24.2 |
| G7 (5625) | “complete system” → “integrated system” | held-out flagship workflows must exercise subsystem integration | shell §27 G7 |
| §29.2 (5896) | “complete day-to-day operating procedure” → “full day-to-day operating procedure” | `AGENTS.md` remains the separate operating manual | shell §29.2 |
| §29.6 (5961) | “complete affected test envelope” → “entire affected test envelope” | broad rewrites still require syntax awareness, reviewed matches, and all affected tests | shell §29.6 |
| §31.6 (6432) | “considered complete” → “satisfies its declared envelope” | held-out extension trial begins only after the candidate meets its stated gate | shell §31.6 |
| §31.6 continuation (6461) | “competitive-supremacy center” → “competitive-comparison center” | Flagships G/H remain the relevant comparison campaigns | shell §31.6 continuation |
| §32 scope risk (6554) | “few/two complete user workflows” → “few end-to-end/two user workflows” | freeze expansion until two workflows close end to end | shell §32 |
| §32 font risk (6560) | “core remains incomplete” → “core remains unfinished” | high-resolution/unhinted fallback and deferral stay unchanged | shell §32 |
| §34.2 (6878) | named supremacy addendum / “did not complete” → “competitive-claim and ambition addendum” / “did not satisfy the full ... gate” | one-input integration does not satisfy multi-plan comparative synthesis | Constitution §34.2 |
| §34.3 lens (6884) | “workflow completeness” → “workflow coverage” | fresh review still asks about every workflow, degraded mode, and refusal | Constitution §34.3 |
| §34.3 self-containment (6903) | “complete user workflows” → “every promised user workflow” | self-containment requires the entire promised workflow set | Constitution §34.3 Self-containment |
| §34.5.3 (6980) | “complete comparative synthesis / Completion” → “satisfy comparative synthesis / Execution” | prior merge remains insufficient; the actual §34.1–34.2 protocol is required | Constitution §34.5.3 |
| Appendix D `MODEL` (7795) | “not a complete spec” → “not a standalone specification” | Arlington remains complementary and non-authoritative by itself | shell Appendix D |
| Appendix G question 9 (7909) | “complete memory-safe ... plan” → “full-coverage memory-safe ... plan” | standing review still challenges JPX/JBIG2 scope credibility | shell Appendix G |

## 15. R14 SELF-CHECK evidence

All checks were rerun locally after SELF-CHECK failure 1 was repaired; no pre-failure pass was carried forward. A passing check establishes only its named relation. It does not validate reviewer grades/process, ratify D1/D2/D3, supply licensed standards meaning, execute a comparison, or strengthen a campaign claim.

| Check | Reproduced result |
|---|---|
| Extraction and feature preservation | Rev 7 has `5,266` unique nonblank lines and `554` unique level-2–4 headings. Missing headings: `0`. Exact-line exceptions: `142`. The report register has `18` extraction/fix + `43` Rule-13 + `78` schema + `3` R13 entries, no duplicate source line, and exact set equality with all 142 exceptions. |
| Normative homes and §34.9 | Stable kernel sentinels occur only at their Constitution homes; the duplicated §30.1.1 heading is an explicit shell `generated-echo` whose body says the Constitution is the sole normative home. Shell §27.1/§27.4 remain only in the shell. The supplied Work Order §34.9 body matches line-for-line through the sealed-cycle closing sentence; the new Gauntlet binding follows it. G0–G7 plus G6.1 are present once each. |
| Schema examples | Exactly five fenced JSON values decode. Every example has the common typed semantic/run identity envelope and one-way backlink. B.1 retains all prior fields and passes interval/security/metadata assertions; B.2 retains both telemetry values (`22.1`, `18432122`) outside semantic identity and passes all new render assertions; B.3 prior role/root/view/expected-state fields pass; B.4 has two unique processor records, each with artifact/version/configuration/environment/output, and `processor_output_artifact_ids` exactly equals the unordered bound-output set; B.5 checker/availability/coverage fields pass. |
| SpecCard registry and marker law | Registry rows: `43`; exact sequence `MB-SC-R0-001`–`043`; each row has exactly three marker cells (`129/129`); empty-template rows: `21`, all exactly `PENDING-LICENSED-SOURCE`; semantic bodies: `0`. Slots 041–043 and the A04/B08 edges are present. Slot 043 is absent from the caller-credential decryption row. |
| R14 targeted regressions | Exactly 19 report rows reproduce `A=5`, `B=10`, `C=4`. A01–A05, B01–B10, and C01–C04 sentinels and loss guards pass. Every B/C report row remains explicitly “not promoted.” The old B.4 field is absent from the Constitution/shell; the new field occurs exactly three times. `ChildContextStorage` occurs zero times in the Constitution and remains three times in shell Appendix A.2. |
| Canonical inputs | All `9/9` values in `ledger/CANONICAL_HASHES.md` match SHA-256. No canonical input was edited. |
| Claim vocabulary | Constitution/pipeline/shell remain `160` matching lines / `202` occurrences: `best=24`, `complete=113`, `fastest=0`, `mogged=1`, `supremacy=64`. The 114 added content-artifact lines have zero hits for that vocabulary; report/checkpoint uses occur only inside allegation, loss-guard, source-before/after, or enforcement structures. |
| Clean room and phase | Added Constitution/pipeline/shell lines have zero prohibited-processor-name hits and zero URL hits. No web/search/fetch, prohibited source/documentation contact, comparison/measurement, SpecCard semantic body, code, build scaffold, Bead/pseudo-Bead, external action, branch, worktree, stash, reviewer spawn, R15 bind, review route, commit, or push was performed by G1. |
| Path and shared-state scope | Against admitted baseline `353d72e0d52b831415af7035d593b85991560760`, exactly the five exclusive G1 paths changed and there are no untracked files. Concurrent root/orchestrator work moved `main`/`origin/main` to `4aa7e83d84fd5b1b92925da49b9aa8c069ef7b10`; the four G1 baseline blobs are identical between those commits. Three live unrelated dirty paths remain untouched and are not G1 evidence. |
| Formatting and diff | G1-baseline and whole-worktree `git diff --check` both pass. All five G1 files are valid UTF-8 with required provenance/status fields, balanced fences, no trailing whitespace, no conflict markers, and no out-of-scope G1 diff. Content-artifact diffs were inspected separately against the admitted baseline. |

### R14 diagnostic failures and corrections

- The first unbounded exception-set display exceeded its result budget and triggered context compaction. `AGENTS.md` was reread immediately; the set was rerun as four bounded ordinal slices. A parallel targeted-search aggregate and a multi-range canonical aggregate also truncated and were replaced by smaller bounded reads.
- The first pre-entry wrapper failed JavaScript parsing before a shell check ran because literal Markdown fences terminated its wrapper string. Two subsequent in-memory JSON regex attempts found zero blocks because the input scalar was initially unset and then the newline was over-quoted. The corrected decoder parsed five values.
- The first extraction/register script used a Unicode-em-dash heading literal that did not compare under its command/file decoding path. ASCII stable-section anchors fixed that diagnostic. The corrected check then found a substantive regression: Rev 7 lines 1872 and 1999 had been expanded in place. The FSM moved `SELF-CHECK → REVISE`; both lines were restored verbatim, R14 additions moved adjacent, and the suite restarted. The 142-line equality then passed.
- The first normative-home wrapper failed JavaScript parsing on literal backticks. Its replacement used one wrong §30.1.1 heading and then an over-specific Unicode echo matcher; both failed rather than passing. Exact local headings and ASCII body sentinels produced the final pass.
- The first schema wrapper accidentally exposed a Perl `${f}` expression to JavaScript template interpolation and failed before parsing. The replacement avoided wrapper interpolation and passed all assertions.
- The first registry regression assumed an expanded `023`–`031` range would spell out slots 029 and 031. It failed. The corrected check validates the inclusive range endpoints, the new explicit edges, and absence of slot 043 from decryption.
- The first canonical-hash wrapper failed JavaScript parsing on literal Markdown backticks. The byte-equivalent `\x60` parser then matched and verified all nine rows.
- Three path/format Perl attempts had the same malformed UTF-8 open-mode quote and failed compilation. The check was split into a raw-byte UTF-8 decoder and a separate git-path validator; both passed. The first failure's subsequent read-only shell lines also exposed the concurrent HEAD move, which was separately verified and persisted.
- The first post-Section-15 bounded register rerun admitted the table-header digit in “Rev 7” as a schema-source line, yielding a false `79` schema entries and `143` total. Restricting that parser to the B.1–B.5 data rows reproduced `78` schema entries, `142` total, and exact registered/actual set equality. The failed parser result was not credited as evidence.
- No failed diagnostic was credited as evidence. The live Round Log, orchestration ledger, and run ledger changed concurrently after intake; G1 did not read their new content or silently reconcile them with the admitted R14 baseline.
