---
generated-by: Codex G1 owner
date: 2026-07-14
inputs:
  - AGENTS.md
  - INDEX.md
  - MONKEYBEE_CAMPAIGN_CHARTER_v1.md
  - OVERNIGHT_GOAL.md
  - CYCLE_0_WORK_ORDER.md
  - AUDIT_FINDINGS_LEDGER.md
  - ledger/prompts/OWNER_MARCHING_ORDERS.md
  - DISPUTES.md
  - decisions/D1_CONTENT_HASH.md
  - decisions/D2_CANONICAL_SERIALIZATION.md
  - decisions/D3_DECODED_STREAM_IDENTITY.md
  - decisions/D4_EVIDENCE_CHECKER_ISOLATION.md
  - decisions/D5_PROJECT_LICENSE.md
  - decisions/D6_LEDGER_BACKEND.md
  - decisions/D7_RUST_TOOLCHAIN_PIN.md
  - decisions/DECISION_INDEX.md
  - decisions/DEFERRED_REGISTER.md
  - "gauntlet/ROUND_LOG.md (bounded R14 lines 1815-2081 only)"
  - "gauntlet/ROUND_LOG.md at commit 3d866bd00d518414bc0fd04b6548c205c0197671 (R15 bytes [544857,573917) and lines 486-500 only)"
status: PROPOSED
evidence-status: provisional-pending-substrate
---

# G1 Owner State

- owner: `Codex G1 owner`
- subgoal: `G1 — Constitution v8, shell, and fix application`
- phase: `DONE/HANDOFF`
- checkpoint: Mandatory hardening and final stable gate pass with no artifact repair. The four artifacts remain byte-identical at manifest `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`. R17/R18 remain explicit unreviewed omissions pending morning ratification/disclosure; the scope cut supplies no substantive validation.
- declared outputs: `constitution/**`, `shell/**`, `reports/FIX_APPLICATION.md`, and `ledger/owners/G1_STATE.md`

## Transition record

| Phase | Timestamp | Checkpoint |
|---|---|---|
| `INGEST` | 2026-07-14T16:31:34+02:00 | Governing instructions read in full; authority, scope, unavailable inputs, and next transition recorded before broader work |
| `DRAFT` | 2026-07-14T16:35:26+02:00 | Dispute handling, G0 verification, full Rev 7 heading inventory, kernel sections, and every available OPEN-C0 fix locus read; minimal output architecture selected |
| `SELF-CHECK` | 2026-07-14T16:58:09+02:00 | Constitution, shell, SpecCard pipeline, and fix map drafted; D-004/D-005 and all G2 decisions reread; all 160 remaining Rule 13 hit lines manually dispositioned and the 43 ordinary hit-bearing lines rephrased |
| `SUBMIT-FOR-REVIEW` | 2026-07-14T17:11:08+02:00 | Final structural and semantic suites pass; all five owned artifacts marked `PROPOSED`; no commit, push, external action, reviewer routing, or out-of-scope edit performed |
| `REVISE` | 2026-07-14T20:42:44+02:00 | Replacement owner resumed the durable FSM without restart and persisted the complete bounded R13 intake before any artifact edit; each filed premise remains unproved pending local authority-chain testing |
| `SELF-CHECK` | 2026-07-14T20:49:54+02:00 | A01-A03 qualified; B01 qualified without promotion; B02 qualified only in its narrower locally supported form. Repairs are limited to Constitution §§9-10, shell Appendix B, and fix-map traceability; process-attestation provenance was corrected append-only before this transition. |
| `SUBMIT-FOR-REVIEW` | 2026-07-14T20:58:33+02:00 | Replacement-owner structural, semantic, loss-guard, traceability, clean-room, formatting, and manifest suites pass. No reviewer was spawned or routed; R14 remains unassigned. |
| `REVISE` | 2026-07-14T21:51:42+02:00 | R14 replacement takeover persisted before artifact edits: pushed baseline commit, component/manifest/checkpoint/log/suffix identities, exact replacement process identity, process quarantine, permitted reads, and 19 allegation/loss-guard records admitted; grades remain metadata and every premise remains unconfirmed. |
| `SELF-CHECK` | 2026-07-14T22:04:58+02:00 | Independent local tests qualified all 19 allegations, with A02/B06/C04 narrowed. Minimal repairs and the 142-line/43-line traceability interfaces are persisted; all filed grades and loss guards remain unchanged. |
| `REVISE` | 2026-07-14T22:06:29+02:00 | Extraction/register equality failed on exactly two newly replaced source lines: Rev 7 §10.6 lines 1872 and 1999. Restore both verbatim and place R14-B04/B01 additions beside them; do not expand the exception register. |
| `SELF-CHECK` | 2026-07-14T22:07:19+02:00 | Rev 7 lines 1872 and 1999 restored byte-for-byte; R14 additions moved adjacent. Reproduced 5,266 unique source lines, 554 headings, zero missing headings, exactly 142 exceptions, and exact `18+43+78+3` register equality. |
| `SUBMIT-FOR-REVIEW` | 2026-07-14T22:22:11+02:00 | The restarted extraction/normative-home/feature-preservation/schema-example/registry-marker/canonical-hash/claim/clean-room/path/diff suite passes after every disclosed diagnostic correction. Final four-component manifest: `dd5266aa80bd0ecdef7780fa670689427e70faee63d279efa1561e0c4e24d8e9`. No reviewer spawn, route, commit, push, R15 bind, or external action occurred. |
| `REVISE` | 2026-07-14T22:28:44+02:00 | Root's independent handoff review found one navigation defect, not an artifact defect: the preserved R13 section's bare `Current checkpoint` heading can be mistaken for live state despite its historical R14-unassigned statements. Qualify that historical surface without changing the four frozen artifacts, manifest, finding dispositions, or prior checkpoint facts. |
| `SELF-CHECK` | 2026-07-14T22:29:08+02:00 | The R13 heading and note now mark its R14-unassigned/terminal statements historical and superseded; verify one live state surface, nonconflicting phase/next-round routing, file integrity, scoped diff, and unchanged four-artifact manifest. |
| `SUBMIT-FOR-REVIEW` | 2026-07-14T22:30:48+02:00 | Navigation check passes with one live state surface, zero bare `Current checkpoint` headings, all four R14-unassigned statements historically qualified, zero phase/next-round conflicts, and R15 named next. UTF-8, fences, whitespace, conflict-marker, and state-only diff checks pass; frozen manifest remains `dd5266aa80bd0ecdef7780fa670689427e70faee63d279efa1561e0c4e24d8e9`. Stop idle; no review route. |
| `REVISE` | 2026-07-14T23:42:30+02:00 | Human-routed R15 takeover admitted from pushed control commit `3d866bd00d518414bc0fd04b6548c205c0197671`: exact four-component/checkpoint identities, committed reviewer byte slice, committed mechanical lines 486–500, process qualification, and 21 allegation/loss-guard rows are persisted before artifact edits. Every premise remains unconfirmed pending independent local test. |
| `SELF-CHECK` | 2026-07-14T23:57:08+02:00 | All 21 independent dispositions and their bounded repairs are persisted: `QUALIFY=17`, `QUALIFY-NARROW=4`, `REJECT=0`, `PREMISE-UNRESOLVED=0`. Begin the full extraction/normative-home/feature-obligation/schema-example/registry-marker/canonical-hash/claim/clean-room/path/diff suite from its first gate; explicitly disposition every new Rule-13 vocabulary hit. |
| `REVISE` | 2026-07-15T00:08:18+02:00 | SELF-CHECK failure 1: the post-report clean-room delta scan returned one task-tracker vocabulary hit because the new enforcement row named the prohibited category while claiming its count was zero. No product/schema premise failed. Replace only that self-referential label, disclose the failed scan, and restart every gate. |
| `SELF-CHECK` | 2026-07-15T00:08:52+02:00 | The report now uses a non-self-matching task-inventory label and records failure 1; the contradictory no-restart sentence is corrected. Restart extraction/normative-home/feature-obligation/schema-example/registry-marker/canonical-hash/claim/clean-room/path/diff from Gate 1. |
| `REVISE` | 2026-07-15T00:11:43+02:00 | SELF-CHECK failure 2: concurrent root work moved `HEAD`/`origin/main` from control commit `3d866bd…` to `7cffe6af…` and reduced the unrelated dirty set to four. Commit-to-commit comparison shows no changed owned path, but the untimestamped report path row is stale. Correct only that shared-state record and restart every gate. |
| `SELF-CHECK` | 2026-07-15T00:12:12+02:00 | Report §16 now records current `7cffe6af…`, the byte-identical owned baselines, and the four current unrelated dirty paths. Restart extraction/normative-home/feature-obligation/schema-example/registry-marker/canonical-hash/claim/clean-room/path/diff from Gate 1. |
| `SUBMIT-FOR-REVIEW` | 2026-07-15T00:19:32+02:00 | The second restarted suite passes extraction membership, normative homes, feature/loss guards, schemas, registry markers, canonical hashes, controlled vocabulary, clean-room, formatting, path, and diff gates. Freeze the four artifacts and stop idle; no reviewer route or external action. |
| `REVISE` | 2026-07-15T00:30:18+02:00 | Root handoff allegation independently qualifies: report §16's path/shared-state row is unqualified present-tense prose, while a name-only current status now shows seven non-G1 dirty paths including later authorized G7 control paths. Time-scope that historical observation only; findings, grades, dispositions, and substantive artifacts remain frozen. |
| `SELF-CHECK` | 2026-07-15T00:30:46+02:00 | Report §16 now time-scopes its exact four-path observation to the final gate at `2026-07-15T00:19:32+02:00` and disclaims any continuing assertion about later concurrent state. Begin the bounded stable suite; any failed relation requires a persisted return to `REVISE`. |
| `REVISE` | 2026-07-15T00:32:44+02:00 | Bounded SELF-CHECK failure 1: the aggregate health wrapper emitted `newline=0` for all five owned files. No newline relation or other same-run result is credited. Determine whether this is a predicate defect; frozen Constitution/pipeline/shell bytes may not change. |
| `SELF-CHECK` | 2026-07-15T00:33:06+02:00 | Direct pre-decode probes show terminal byte `10` and valid UTF-8 for every owned file. The failed wrapper tested a scalar consumed by `Encode::decode`; no artifact defect exists. Restart every bounded gate with newline measured before decoding. |
| `SUBMIT-FOR-REVIEW` | 2026-07-15T00:35:14+02:00 | The restarted bounded suite passes every required relation. Report §16's four-path statement is timestamped and explicitly non-continuing; frozen substantive hashes match, and only report/checkpoint changed in this turn. Report `06faddd7…`; manifest `24327520…`. Stop idle; no R16 route. |
| `INGEST` | 2026-07-15T13:09:16+02:00 | Begin the bounded R51 follow-up sub-FSM. `HEAD` and `origin/main` equal required control `cf932eabc47dca8d42b0286995d670a94299f590`; all submitted line/byte/hash tuples, manifest, and checkpoint tuple match exactly. Evidence analysis and disposition remain gated behind this persisted state. |
| `DRAFT` | 2026-07-15T13:10:54+02:00 | Root sequencing correction: post-`INGEST`, pre-`DRAFT` R51 marker search, committed range display, blob count, and all derived byte/hash probes are zero-credit raced work. No artifact or disposition mutation occurred. Rerun all required R51 evidence reads from disk before any premise use. |
| `SELF-CHECK` | 2026-07-15T13:14:15+02:00 | Credited DRAFT rereads reproduce the R51 packet/control identities, zero allegation IDs, corrected transcript-bound timing record, and six disclosed variances. The informational §34.4/§34.9 premise is supported only as a human-ratification question; artifact-repair count remains zero. Begin fresh terminal checks. |
| `REVISE` | 2026-07-15T13:15:37+02:00 | R51 SELF-CHECK failure 1: UTF-8-aware navigation check found the R51 run-record heading labeled `INGEST` while top-level live phase is `SELF-CHECK`. Correct only the ambiguous heading, then restart the entire terminal suite. |
| `SELF-CHECK` | 2026-07-15T13:15:56+02:00 | The R51 run-record heading is phase-neutral; historical `INGEST`, `DRAFT`, `SELF-CHECK`, and `REVISE` transitions remain append-only. Restart all terminal checks from disk with no pre-failure credit. |
| `SUBMIT-FOR-REVIEW` | 2026-07-15T13:17:48+02:00 | Restarted terminal suite passes: four artifact tuples and manifest unchanged; R51 control/packet identities and six variances reproduced; zero allegation IDs; navigation, UTF-8/LF/CR/whitespace/conflict/fence, clean-room, controlled-word, registry, scoped-path, and diff gates pass. No R17 action. |
| `DONE/HANDOFF` | 2026-07-15T13:19:58+02:00 | Submission gate remained stable on disk. The warning-producing post-submission delta scan is zero-credit; its initialized-counter rerun passes. Persist terminal root handoff before final read-only identity verification; no reviewer or R17 action. |
| `REVISE` | 2026-07-15T13:22:08+02:00 | Terminal diagnostic failure 2: a wrapper incorrectly applied the recorded `163/205` controlled-vocabulary baseline to all four artifacts by adding the report to its scope. The observed `177/238` result proves no defect. Zero-credit the wrapper; correct only its scope and restart bounded checks. |
| `SELF-CHECK` | 2026-07-15T13:22:26+02:00 | No artifact repair was needed. Restart the bounded terminal suite from its control bracket with corrected diagnostic scope; carry forward no result from the failed aggregate. |
| `REVISE` | 2026-07-15T13:23:19+02:00 | Terminal SELF-CHECK failure 3: exact packet bytes/hash matched, but a guessed field-format regex returned zero verdict/termination matches and forced a nonzero exit. Credit no result from the wrapper. Correct only the verifier against the sealed syntax, then restart all bounded gates. |
| `SELF-CHECK` | 2026-07-15T13:23:40+02:00 | The exact permitted packet range shows one `Round verdict` heading and one `termination state: TERMINATED` field. No artifact repair was needed. Restart all bounded terminal gates with literal syntax checks. |
| `REVISE` | 2026-07-15T13:24:43+02:00 | Terminal SELF-CHECK failure 4: the three-file controlled-vocabulary wrapper imposed whole-word boundaries and returned `133/168`, while the persisted `163/205` relation uses substring matching. Credit no result from the wrapper or same restart. Restore the recorded matcher and restart all bounded gates. |
| `SELF-CHECK` | 2026-07-15T13:25:01+02:00 | Root confirms the original baseline matcher: case-insensitive controlled-token substrings over exactly Constitution, SpecCard pipeline, and shell. Keep the report hash separate; restart the bounded gate once with no speculative diagnostic. |
| `SUBMIT-FOR-REVIEW` | 2026-07-15T13:26:33+02:00 | The bounded restart passes from a stable control bracket: exact artifact identities/manifest, sealed control/packet identities, zero allegation IDs, phase-neutral navigation, file hygiene, scoped diff, original `163/205` matcher relation, and initialized checkpoint-delta guards all pass. No artifact repair, reviewer action, or R17 action. |
| `DONE/HANDOFF` | 2026-07-15T13:27:42+02:00 | Post-submission identities, navigation, hygiene, scoped diff, and initialized delta guards pass. Persist terminal root handoff before final read-only hash reporting; all submitted artifacts remain frozen and no reviewer or R17 action is authorized. |
| `SUBMIT-FOR-REVIEW` | 2026-07-16T02:01:50+02:00 | Live human override reopens the prior disk `DONE/HANDOFF` checkpoint for a mandatory hardening pass. The reported bounded landing is 22 accepted reviews against an overnight target of approximately 20 while preserving the 12-accepted-C1 floor. R17/R18 are cut as unreviewed omissions, not passed evidence. |
| `REVISE` | 2026-07-16T02:02:17+02:00 | Mandatory hardening begins. Reproduce owner-contract, admitted-disposition, integrity, pending-source, controlled-vocabulary, clean-room, no-action, scope, and manifest relations before deciding whether any artifact correction qualifies. R17/R18 remain explicit unreviewed omissions. |
| `DONE/HANDOFF` | 2026-07-16T02:07:48+02:00 | Mandatory hardening and post-append stable gate pass with artifact repairs `0`. Existing admitted evidence remains bounded to its recorded reviews; R17/R18 remain unreviewed omissions with no findings, grades, pass state, lens coverage, or convergence credit. Stop idle for root/morning disclosure. |

## R15 bounded packet receipt — persisted before artifact edits

- owner route: continuing G1 owner in session `monkeybee-pdf-mass-context-repo--g1b`, exact process `gpt-5.6-sol`, reasoning effort `ultra`, live service tier `priority`, live fast mode. This identifies the owner process and supplies no finding evidence.
- authoritative control: pushed commit `3d866bd00d518414bc0fd04b6548c205c0197671`; local `HEAD` and `origin/main` each reproduced that identity before this write.
- admitted pre-edit components:
  - `b5b5d673b64732ebd9efa85d3183a6e17c7ce167226eed1b15755aaf3e9cd0a9  constitution/MONKEYBEE_CONSTITUTION_v8.md`
  - `6d3a9ddb68f2f9c3f8e3a169a9b44cd9513a91a60ad3e848e0b0d3357a88c877  constitution/SPECCARD_PIPELINE.md`
  - `09cf724620fb8173640ed542a1ed18277e09eafb8be62b0302c93a9ec8f47739  shell/MONKEYBEE_SHELL_CORPUS_v8.md`
  - `62e72bae29fe1e258f6b97a3719dbad7123307d1ef92e893749cece8c5005d77  reports/FIX_APPLICATION.md`
- admitted sorted-manifest SHA-256: `dd5266aa80bd0ecdef7780fa670689427e70faee63d279efa1561e0c4e24d8e9`; ordinary two-space `sha256sum` lines, bytewise sorted, final newline retained.
- admitted G1 checkpoint: phase `SUBMIT-FOR-REVIEW`, SHA-256 `c2ce61fb9a54f6a0543bbfb773222412d1e4def5e8e66ded9f7bd5f2cb36a5d7`.
- committed reviewer bytes: `git show 3d866bd00d518414bc0fd04b6548c205c0197671:gauntlet/ROUND_LOG.md` through `dd bs=1 skip=544857 count=29060 status=none`; committed range `[544857,573917)`, exactly `29,060` bytes / `191` lines / SHA-256 `610bc2ec58c899cad9b14da7097ebacca457c0987f237edfd5d45737f2d84595`.
- committed mechanical validation/routing: only the same committed blob's lines `486–500` were displayed. Those lines mechanically record their earlier sealed-coordinate namespace, schema count, timestamp correction, chain qualification, process/termination telemetry, and bounded route. This owner admits no substantive conclusion from them.
- live-log boundary: the live `gauntlet/ROUND_LOG.md` was not opened. No committed Round Log byte outside the exact reviewer slice and lines `486–500` was displayed.
- reviewer-process status: `FILED · CHAIN-QUALIFIED · TERMINATED`; `A=14`, `B=7`, `C=0`; `21` allegations; every grade and loss guard remains filed and unpromoted. Process qualification establishes and refutes no premise.
- disclosed process deviation: four extra goal lines exposed only G1 paths/priority; a mistakenly named audit-ledger search returned no audit content. Neither is used as premise evidence; residual priming risk remains non-zero.
- continuing-checkpoint read: the first whole-file display of `ledger/owners/G1_STATE.md` exceeded the result bound and was not credited as a full read. Three contiguous reads, lines `1–120`, `121–240`, and `241–380`, then covered the complete 353-line file.
- concurrency boundary: the human reports active G3 files concurrently dirty. G1 will not open, touch, stash, revert, reconcile, or cite them.
- write boundary: only the four declared G1 artifacts and this owner checkpoint are writable, always through structured `apply_patch`. No artifact had been edited when this receipt and transition were persisted.

| Finding | Filed grade | Unconfirmed routed allegation | Preserved loss guard | Intake state |
|---|---|---|---|---|
| `R15-A01` | `A` | The staged R0 envelope allegedly omits conditional caller-credential read decryption. | Preserve inventory-only operation, absent-credential/key refusal, public-key-handler exclusion, and the no-writer boundary. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A02` | `A` | `OpenReport` allegedly lacks an auditable credentialed-decryption operation outcome. | Expose no password, key, or credential-derived secret; retain inventory-only and refused outcomes as first-class states. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A03` | `A` | `OpenReport` allegedly drops header evidence and per-revision dialect history. | Preserve pre-header bytes, conflicts, recovery assumptions, and revision-local history; add no timeless document-wide version scalar. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A04` | `A` | `RenderReport` allegedly omits source-root and conditional source-integrity facts. | Never emit a whole-source content claim from partial availability; preserve graph/view/derivation identities and telemetry separation. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A05` | `A` | `TransformReceipt` allegedly omits safe apply-time authority, state, idempotency, atomicity, crash, and publication facts. | Keep transient authority/idempotency/publication facts out of `TransformRoot` identity and preserve private-candidate versus independent-admission separation. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A06` | `A` | `ValidationReport` allegedly fails to bind the checker implementation that scopes its verdict. | Preserve per-rule outcome, separate severity/coverage, unsupported-rule inventory, and independent observations without treating them as a normative oracle. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A07` | `A` | Generated Appendix B examples allegedly omit mandatory common semantic-body fields. | Preserve every family field, one-way observation backlink, telemetry exclusion, and pending D1/D2 markers. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A08` | `A` | B.1 allegedly makes unreconciled statements about an unexplained source range. | Retain independent overlapping interval facets; do not delete the detailed range ledger. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A09` | `A` | B.5 allegedly omits checker isolation, seeded-trial, and lineage/correlation evidence. | Preserve `PROPOSED` and unavailable states; never infer independence from architecture, names, or package-parse success. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A10` | `A` | The empty SpecCard interface allegedly collapses independent rights decisions into generic provenance. | Keep every semantic value exactly `PENDING-LICENSED-SOURCE`, preserve separate technical/rights review, and add no source meaning or licensed prose. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A11` | `A` | The contract-generated claim registry allegedly lacks a declared identity/linkage interface. | Preserve one `ConsequenceContractId` per atomic operation/profile/subclaim, typed composition, lifecycle/no-claim boundaries, and the no-strengthening rule. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A12` | `A` | Shell Appendix E allegedly omits fields required by its consequence-contract-derived release matrix. | Preserve per-profile staging, exclusions, typed refusal/indeterminate outcomes, and separately scoped comparison rows without widening a release claim. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A13` | `A` | The fix report allegedly weakens the mandatory second forensic path to optional. | Preserve lineage disclosure, correlation accounting, and the rule that shared-lineage paths do not count as two independent falsifiers. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-A14` | `A` | The unverified standards paragraph allegedly asserts external confirmation unavailable to this no-fetch run. | Preserve identifier-only planning baseline, edition/profile distinctions, no-fetch disclosure, and all pending-source gates. | `PENDING-INDEPENDENT-TEST`; grade/process prove nothing. |
| `R15-B01` | `B` | The C1 gate allegedly invokes all Workflow B despite C1's excluded later-layer features. | Do not move rendering/fonts into C1 and do not delete the later full Workflow B outputs. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |
| `R15-B02` | `B` | The R0 SpecCard coverage denominator allegedly depends circularly on post-hoc implementation/hypothesis discovery. | Preserve the insufficiency caveat, allow licensed extraction to split/add slots, and keep every semantic body pending-source. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |
| `R15-B03` | `B` | A rights change allegedly creates a successor card without disabling inadmissible use of the old version. | Preserve immutable historical versions, review evidence, and technical meaning; never rewrite an old card or infer new rights. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |
| `R15-B04` | `B` | Workflow Q allegedly lacks a report-family home for external-recognition evidence. | Preserve native/recognized alternatives and conflicts, hostile-data treatment, least disclosure, confidence no-laundering, and non-recomputation boundaries. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |
| `R15-B05` | `B` | The human staffing plan allegedly has no stable, checkable artifact interface. | Preserve survival-wedge downgrade, human/agent distinction, conflict disclosure, and refusal to infer independence from nominal roles. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |
| `R15-B06` | `B` | The C1 `G0–G3-subset` gate allegedly has no enumerated assurance subset. | Preserve the full G0–G7/G6.1 taxonomy for later cycles and do not imply C1 passed later tiers. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |
| `R15-B07` | `B` | Exact unique-line membership allegedly carries a broader feature-preservation label than its measured relation. | Preserve canonical hash bind, 142-exception partition, stable coordinates, exact-line evidence, and the no-strengthening rule for automated checks. | `PENDING-INDEPENDENT-TEST`; remains Grade `B`. |

- intake arithmetic: `14 + 7 + 0 = 21`; monotone IDs `R15-A01`–`A14`, `R15-B01`–`B07`; this intake table remains the pre-test receipt and is superseded for premise status only by the disposition table below.

## R15 independent premise dispositions — persisted before artifact edits

The reviewer process, wording, scope, and grades were not used as evidence. The local tests below use the Charter/Work Order/Audit chain, the current Constitution and shell homes, the empty SpecCard pipeline, the fix report, and only demand-driven canonical Revision 7 sections. A premise disposition authorizes no repair outside its stated boundary.

| Finding | Filed grade | Disposition | Local evidence and independently tested premise | Smallest authorized repair boundary | Loss guard that must survive |
|---|---|---|---|---|---|
| `R15-A01` | `A` | `QUALIFY` | Charter C1 scope and Work Order §7 require caller-credential standard-handler read decryption, while shell §3.0 names inventory and refusal without credentials but no positive conditional read path. | Add one R0 release-envelope clarification adjacent to shell §3.0; do not rewrite the canonical table row. | Inventory-only remains valid; absent credential/key refuses; public-key handling and every writer capability remain excluded. |
| `R15-A02` | `A` | `QUALIFY` | Constitution §10.6 exposes only an encryption-state field; the C1 read-decryption operation has no typed request/admission/outcome/coverage/evidence record. | Add the operation record to Constitution §10.6 `OpenReport` and regenerate B.1 only as required by that field. | No password, key, or credential-derived secret enters a report; inventory-only, refusal, partiality, and indeterminacy remain explicit. |
| `R15-A03` | `A` | `QUALIFY` | Canonical/shell §§12.3–12.3.1 require header candidates, pre-header bytes, conflicts, and revision-local `EffectiveDialect` history; current §10.6 `OpenReport` and B.1 do not carry that interface. | Add the two family fields in Constitution §10.6 and their generated B.1 values. | Preserve exact header evidence, recovery assumptions, unresolved conflicts, and per-revision history; add no timeless version scalar. |
| `R15-A04` | `A` | `QUALIFY` | Shell Workflow A requires `SourceRootId`, availability/integrity class, and a whole-source content identity only when coverage/authentication permit it; current `RenderReport` starts at view/graph/derivation. | Add a conditional source-identity field to Constitution §10.6 and B.2. | Partial availability never yields a whole-source content claim; graph/view/derivation identity and telemetry separation remain unchanged. |
| `R15-A05` | `A` | `QUALIFY-NARROW` | Canonical/shell §25.9 requires apply-time state/authority recheck, atomicity, durable idempotency, crash reconciliation, and publication reporting. Constitution §10.6 lacks those facts, but its `TransformReceipt` is deliberately a private candidate rather than the authority itself. | Add a separately typed, proposal-driven apply-admission subrecord to `TransformReceipt` and B.3; candidates not produced by apply mark it not applicable. | Transient authority/idempotency/publication facts never enter `TransformRoot`; private-candidate and later independent-assurance admission remain separate. |
| `R15-A06` | `A` | `QUALIFY` | Canonical/shell §24.1 scopes a validation verdict to checker build and rule implementation versions; current `ValidationReport` binds profile/rule set but not those identities or its validation derivation. | Add validation `DerivationId`, checker build identity, and per-rule implementation version fields to Constitution §10.6. | Preserve canonical per-rule outcomes, separate severity/coverage, unsupported inventory, and non-normative external observations. |
| `R15-A07` | `A` | `QUALIFY` | Constitution §10.8 mandates canonical outcome, coverage, deterministic diagnostics, evidence, and no-claim fields. One or more of those explicit common fields is absent in each generated B.1–B.5 value; §10.10's generator sentence also omits them. | Close the §10.10 generator/validator list and add only missing common fields to B.1–B.5. | Preserve every family field, one-way observation backlink, telemetry exclusion, and all D1/D2 pending-ratification markers. |
| `R15-A08` | `A` | `QUALIFY` | B.1 records `unexplained_subranges` as `[1839000,1839021)` while `unexplained_bytes` is empty, and §10.6 gives no reconciliation law. | Define `unexplained_bytes` as the exact normalized projection of that interval facet and correct B.1. | Keep every independent overlapping interval facet; no exclusive enum or ledger deletion is permitted. |
| `R15-A09` | `A` | `QUALIFY` | Constitution §10.6.1 requires five isolation axes, seeded-defect trial results, and lineage/correlation status; B.5 exposes only checker protocol/capability plus package coverage. | Add explicit proposed/unavailable isolation, trial, and lineage fields to generated B.5 and its Appendix-B generator row. | Architecture, names, or parse success never imply independence; unavailable evidence and the `PROPOSED` status remain visible. |
| `R15-A10` | `A` | `QUALIFY` | Constitution §4.2.1 enumerates independent human, tool, model, training, corpus, quotation, redistribution, black-box, encumbrance, and jurisdiction decisions; the empty template collapses them into generic rights provenance. | Add separate empty rights slots and review identities in pipeline §§4–6 only. | Every value remains exactly `PENDING-LICENSED-SOURCE`; technical and rights review stay separate; no source meaning is authored. |
| `R15-A11` | `A` | `QUALIFY` | Charter C1/Work Order §7 require a claim registry; Constitution §10.10 derives claims from contracts but §9.2 and §10.10 define no registry snapshot identity or exact `ClaimId`→`ConsequenceContractId` linkage. | Add `ClaimRegistryId` in §9.2 and a generated atomic-linkage/lifecycle projection in §10.10. | One contract per atomic item, typed composition, withdrawal/no-claim boundaries, and surface narrowing remain; registry prose cannot strengthen a claim. |
| `R15-A12` | `A` | `QUALIFY` | Shell §27.4 requires identity domain, authority/preconditions, outcome/refusal/indeterminate algebra, privacy/availability, expiry, and withdrawal. Appendix E's seven-column human view omits those machine-row fields. | Add a contract-derived companion field envelope after the existing Appendix-E table; preserve every current row. | Per-profile staging/exclusions and typed non-success states remain; comparison rows stay separately scoped and no release claim widens. |
| `R15-A13` | `A` | `QUALIFY-NARROW` | Canonical and current shell §31.4 both require two forensic paths, and report §3 says two; only report §13.1 calls the second path optional. | Correct that one §13.1 accounting cell; make no shell or Constitution change for this ID. | Both paths disclose lineage; shared or unknown lineage remains correlated and cannot count as two independent falsifiers. |
| `R15-A14` | `A` | `QUALIFY` | Constitution §4.4 labels the inherited paragraph unverified and records no source contact, yet the active sentence still says current status was externally confirmed. No permitted local evidence supports that clause. | Replace only the active status clause with identifier-only planning language and retain the exact inherited source line solely in an explicitly quarantined, non-status source-membership record. | Preserve identifiers and edition/profile/errata distinctions, no-fetch disclosure, rights gates, and the exact 142-line extraction partition. |
| `R15-B01` | `B` | `QUALIFY-NARROW` | Charter C1 is expressly L0–L2 and excludes rendering/fonts, while its gate says Workflow B with an `OpenReport` parenthetical; full shell Workflow B also includes later-layer visible/text/glyph outputs. The defect is an ambiguous projection, not a scope transfer. | Add a C1-only L0–L2/OpenReport projection beside shell Workflow B; leave the Charter and full workflow untouched. | Rendering/fonts do not enter C1; later full Workflow B outputs are neither deleted nor represented as C1-pass conditions. |
| `R15-B02` | `B` | `QUALIFY` | Pipeline §3 makes some coverage depend on slots actually implicated by a discovered hypothesis/operation. Its insufficiency caveat permits new slots but does not freeze a prospective pre-gate denominator. | Add a prospective R0 dependency-manifest/closure rule in pipeline §§3–4 without filling a card. | Licensed extraction may split/add slots and reopen a gate; every semantic body remains pending-source and post-hoc coverage cannot define its own denominator. |
| `R15-B03` | `B` | `QUALIFY` | Pipeline §4 step 10 creates a successor after a rights change but does not state that an old technically valid card can become inadmissible for a context under Constitution §4.2.1. | Add an immutable technical-version/separate rights-admissibility lifecycle in pipeline §§4–6. | Historical cards and review evidence are not rewritten; a successor grants no inferred rights and blocked old contexts cannot keep using the old card. |
| `R15-B04` | `B` | `QUALIFY` | Shell Workflow Q requires request/response identity, disclosure, hostile-data validation, alternatives/conflicts, confidence semantics, and replay no-claims. Constitution §10.6 has no report family that jointly owns those facts. | Add an `ExternalRecognitionReport` family and a one-way `TextReport` evidence link in Constitution §10.6. | Native and recognized evidence remain alternatives; least disclosure, hostile-data treatment, confidence no-laundering, and non-recomputation boundaries survive. |
| `R15-B05` | `B` | `QUALIFY-NARROW` | Work Order R1-5 and shell §35.1 require a human-independence staffing plan and downgrade law, but no stable field interface makes an instance checkable. No staffed instance is available or authorized here. | Define the stable plan interface beside shell §35.1 only; do not fabricate personnel, assignments, availability, cost, or a new file. | Preserve human/agent distinction, conflicts/lineage, survival-wedge downgrade, unavailable roles, and refusal to infer independence from role labels. |
| `R15-B06` | `B` | `QUALIFY` | Charter C1 names `G0–G3-subset`; shell §27 defines full G0–G7/G6.1 but no C1 subset or exclusion of later-layer G3 evidence. | Add an explicit C1 assurance projection after shell G3, limited to the declared L0–L2/R0 operation and report contract. | The full taxonomy stays intact; later tiers and render/font/writer evidence are not implied passed; unavailable G3 evidence remains unavailable. |
| `R15-B07` | `B` | `QUALIFY` | Report labels call unique heading/line membership and a registered exception-set equality check feature preservation. Those relations do not by themselves prove contextual, multiplicity, or obligation preservation. | Rename only the report labels and add the exact no-strengthening caveat; retain the obligation registers. | Canonical hash, 5,266/554 coordinates, 142-line partition, stable loci, and exact-line evidence remain unchanged. |

Disposition arithmetic: `14 A + 7 B + 0 C = 21`; `QUALIFY=17`, `QUALIFY-NARROW=4`, `REJECT=0`, `PREMISE-UNRESOLVED=0`; exactly one disposition for every monotone ID. A premise qualification is not a grade promotion; every `B` remains filed Grade `B`.

Post-compaction/process diagnostics before artifact edits:

- Context compaction occurred during authority review. Per AGENTS.md Rule 6, `AGENTS.md` was the first file reread afterward and was read in full before continuation.
- One parallel bounded shell/report read aggregate and one bounded Constitution aggregate exceeded their display budgets. Neither truncated display was credited. Exact smaller ranges and stable heading anchors were then read; no range used as evidence remained dependent on truncated output.
- The first two structured disposition patches failed exact-context verification and changed no file: the first encoded the intake bullet with one missing patch-marker hyphen; the second did not consume the existing blank line before the R14 heading. The checkpoint/intake update and this insertion were then split into exact structured patches.
- The live Round Log remained unopened. No G3 file was opened or touched. No web, fetch, comparison execution, measurement, prohibited source/documentation contact, or repository write other than structured checkpoint patches occurred.

Authorized artifact-edit count is now `21` bounded finding repairs, with overlaps coalesced by home; phase remains `REVISE`.

## R15 repair progress — `REVISE`

- Constitution repair applied for `A02`, `A03`, `A04`, narrowed `A05`, `A06`, `A08`, `A11`, `A14`, and `B04`, plus the Constitution half of `A07`. The active standards sentence is identifier-only/unverified; its exact inherited source line is retained solely in a fenced quarantine record so extraction membership can remain unchanged. Current Constitution SHA-256: `2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`; scoped `git diff --check` passed.
- SpecCard-pipeline repair applied for `A10`, `B02`, and `B03`: 43 registry rows still carry exactly `129/129` pending markers, and all 34 empty-template rows carry exactly `PENDING-LICENSED-SOURCE`. Prospective dependency closure and separate immutable-technical/rights-admissibility lifecycle add no semantic body. Current pipeline SHA-256: `6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`; scoped `git diff --check` passed.
- Shell repair applied for `A01`, the generated-example halves of `A02`–`A05` and `A07`–`A09`, `A12`, narrowed `B01`, narrowed `B05`, and `B06`. Exactly five JSON values decode; all five carry the required common field set and valid one-way backlink. Targeted decryption/no-secret, interval projection, partial-source hash guard, apply-admission, and unavailable checker-independence assertions pass. Current shell SHA-256: `158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; scoped `git diff --check` passed.
- Fix-report repair applied for narrowed `A13`, `B07`, and all R15 traceability: exactly 21 rows reproduce `17/4/0/0`, all seven B rows say not promoted, stale editorial labels are absent, and the 142-line register remains stated without a stronger automated inference. Current report SHA-256: `67efe7f6f2d70af951a205ca22b2286a2adb68f64ff9c78ed487627e01e91f80`; scoped `git diff --check` passed.
- One attempted all-in-one Constitution patch used a nonexistent R13 provenance-header neighbor; `apply_patch` rejected the whole patch and changed no file. The provenance addition and content patch were then applied separately against exact local anchors. No failed patch result is credited.
- No repair home remains pending at the `SELF-CHECK` entry boundary. Any suite defect requires `SELF-CHECK -> REVISE`, one bounded correction, and a restart from the first gate.

## R15 SELF-CHECK failure 1 — 2026-07-15T00:08:18+02:00

- failed relation: the post-report added-line clean-room scan returned `bead_hits=1` and exit `1` after `reports/FIX_APPLICATION.md` §16 was added.
- exact cause: the §16 evidence row used the category label itself in the phrase claiming that category had zero hits. The hit is enforcement prose, not a task inventory, but the stated lexical relation is false and cannot be smoothed.
- bounded correction: replace only that category label with a non-self-matching phrase and append this failure to the report diagnostics. No Constitution, pipeline, shell, finding disposition, grade, loss guard, or other report field is authorized to change.
- restart law: after the correction, transition back to `SELF-CHECK` and rerun the entire suite from extraction membership. No earlier gate result is carried forward.
- correction applied: only the report category label and its now-contradictory no-restart diagnostic sentence changed; failure 1 is appended to report §16. No other artifact content changed.

## R15 SELF-CHECK failure 2 — 2026-07-15T00:11:43+02:00

- failed relation: report §16's path/shared-state row no longer matched the final path read after concurrent root integration.
- reproduced current state: `HEAD` and `origin/main` are `7cffe6af7b363a5f53f4322bf1f0eb703c4fb424` on `main`; no owned path differs between control commit `3d866bd00d518414bc0fd04b6548c205c0197671` and current `HEAD`; four non-G1 paths are dirty and no untracked file exists.
- bounded correction: update only report §16's shared-state row and append this concurrent-event diagnostic. Do not open or cite newly committed Round Log content and do not alter any content repair.
- restart law: after correction, transition to `SELF-CHECK` and restart from extraction membership; no prior restarted-gate result carries forward.
- correction applied: only the report shared-state row, failure-2 diagnostic, and aggregate failure sentence changed. No Constitution, pipeline, shell, finding row, grade, or loss guard changed.

## R15 final SELF-CHECK and submission boundary — 2026-07-15T00:19:32+02:00

The four content artifacts were frozen before the hashes below were computed. This checkpoint is not a manifest member. Each passing relation proves only its stated relation; it does not validate reviewer wording or process, promote a grade, supply licensed source meaning, ratify an identity encoding, execute a comparison or measurement, instantiate staffing, or widen a claim.

### Final disposition and gate arithmetic

- independent R15 dispositions: `21 = A14 + B7 + C0`; `QUALIFY=17`, `QUALIFY-NARROW=4`, `REJECT=0`, `PREMISE-UNRESOLVED=0`; every ID has one disposition and all seven B rows remain filed Grade B and explicitly not promoted;
- extraction membership: Rev 7 unique nonblank lines `5,266`; unique level-2–4 headings `554`; missing headings `0`; exact exceptions/registered loci `142/142`; duplicates/missing/extra `0/0/0`; partition `18+43+78+3`;
- normative homes: Work Order §34.9 matches all `10` normalized lines; required homes and the G0–G7/G6.1 taxonomy are singular; `24` bounded R15 sentinels pass;
- schema examples: exactly `5` JSON values decode and satisfy the family/common-field, one-way backlink, decryption/header/dialect/interval, partial-source, apply-admission, processor-output, and proposed/unavailable isolation/trial/lineage assertions;
- SpecCard boundary: registry rows `43`, sequence `001–043`, markers `129/129`, bad rows `0`; empty-template rows `34`, non-marker values `0`; slot-043 separation and semantic-body count `0`;
- canonical inputs: `9/9` hashes match;
- prior and current loss guards: R14 remains `19 = A5+B10+C4` with `14` B/C rows not promoted; the R15 report/checkpoint arithmetic agrees; `37` cross-artifact loss-guard sentinels pass with failures `0`;
- controlled-vocabulary baseline: Constitution/pipeline/shell remain `163` matching lines / `205` occurrences. The control-commit delta has exactly `5` hit-bearing lines / `9` occurrences: two required outcome enum literals, one precisely scoped C1 OpenReport gate result, one exact 353-line checkpoint-read statement, and the report's five enforcement labels. Report §16 records those dispositions; no editorial schema-praise use remains;
- clean-room delta: prohibited-processor-name, URL, task-inventory, and non-text newly opened-fence hits are all `0`; no web/fetch, prohibited source or documentation, semantic body, comparison/measurement execution, code/scaffold, external action, or reviewer route occurred;
- formatting and scope: all five owned Markdown files decode as UTF-8, carry provenance/status, have balanced fences, trailing whitespace `0`, conflict markers `0`, and pass scoped plus whole-worktree `git diff --check`;
- path state: exactly the five exclusive G1 paths differ from control commit `3d866bd00d518414bc0fd04b6548c205c0197671`; the five owned baseline blobs do not differ between that commit and current `HEAD`; no untracked path exists.

### Frozen four-component manifest

Method: ordinary two-space `sha256sum` lines, bytewise sorted under `LC_ALL=C`, with the final newline retained, then SHA-256 over that byte string.

```text
11c2802e9ada7c8041eb3d740a6f15a8fee6b1160d4d776815124e20bb85690a  reports/FIX_APPLICATION.md
158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315  shell/MONKEYBEE_SHELL_CORPUS_v8.md
2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe  constitution/MONKEYBEE_CONSTITUTION_v8.md
6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1  constitution/SPECCARD_PIPELINE.md
```

- sorted-manifest SHA-256: `68411527df617c9a8af2740b7c371f2bcf895be6dbcaa8ece0c18926030c0f3e`.
- shared-state snapshot at the pre-transition path gate: `HEAD=origin/main=7cffe6af7b363a5f53f4322bf1f0eb703c4fb424`, branch `main`; four unrelated dirty paths remain `ledger/ORCHESTRATION_STATE.md`, `ledger/RUN_LEDGER.md`, `ledger/owners/G3_STATE.md`, and `plans/CYCLE_1_DELTA_PLAN.md`. G1 did not open or change their bodies.

### Additional diagnostics from the second restart

None of the following failed invocations was credited:

1. The first restarted R15-sentinel Perl wrapper named its file helper `s`, which parsed as substitution syntax and failed compilation. Renaming it `slurp` yielded the `24`-sentinel pass.
2. The first schema assertion bound an iterator incorrectly inside jq `all(...)` and failed compilation. Binding the root value before the iterator produced `true` for all five values.
3. The first controlled-word delta scanner enabled strict mode without declaring its accumulators and failed compilation. Declared accumulators reproduced the five-line/nine-occurrence result and the per-use dispositions above.
4. The first loss-guard wrapper used bare `s` as a file key and failed compilation. Its next form reported four false absences because UTF-8 source semantics were missing for punctuation literals and one XML needle did not match the artifact wording. UTF-8 source mode plus the exact local XML phrase produced `37/37` with failures `0`.
5. The first formatting wrapper's added terminal-newline predicate falsely returned zero for all five files even though a direct last-byte probe returned `0a` for each. The required UTF-8/provenance/fence/whitespace/conflict relations passed in the corrected wrapper; no file-ending rewrite was made.
6. The first path wrapper used `chomp` while its input-record separator was locally undefined, leaving a newline in the captured revision and causing `git diff` to reject the revision string. Removing the terminal newline explicitly produced the stable-head/path/diff pass above.

The earlier failed patches and diagnostics, plus SELF-CHECK failures 1–2 and their required full restarts, remain recorded above and in report §16. No failed result is smoothed into a pass. Terminal phase is `SUBMIT-FOR-REVIEW`; the owner stops idle without binding, routing, reviewing, spawning, committing, or pushing.

## R15 post-submission evidence-navigation correction — `REVISE`

- root handoff allegation: section 16 presents its four-path dirty-set observation as a continuing current claim even though it was measured only at the final path gate on `2026-07-15T00:19:32+02:00`;
- independent premise test: the report row starts with unqualified present tense and says four non-G1 dirty paths “remain.” Read-only current path facts show seven non-G1 dirty names: the original orchestration state, run ledger, G3 state, and Cycle-1 delta plan, plus later `gauntlet/CONVERGENCE_REPORT.md`, `gauntlet/ROUND_LOG.md`, and `ledger/owners/G7_STATE.md`. Only names/status were observed; none of those bodies was opened;
- disposition: `QUALIFY-NARROW` as evidence navigation, not an artifact or finding defect. The smallest repair is to prefix the report row with the exact final-gate timestamp and state that the row is a reproducibility snapshot rather than a continuing worktree assertion;
- frozen scope: Constitution `2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`, SpecCard pipeline `6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`, and shell `158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315` remain byte-frozen. R15 remains `A14/B7/C0` with `17` qualifications, `4` narrowed qualifications, and no rejected/unresolved premise; every B stays unpromoted;
- write boundary: only `reports/FIX_APPLICATION.md` and this checkpoint. No substantive artifact, finding row, grade, loss guard, canonical input, or concurrent path is authorized to change.
- bounded repair applied: report §16 now begins the path/shared-state evidence with the exact final-gate timestamp, preserves the then-true `HEAD`/`origin/main`, owned-baseline, no-untracked, and four-path facts, and expressly limits them to a reproducibility snapshot rather than later concurrent worktree state. No other report row or artifact changed.
- bounded SELF-CHECK failure 1: the first aggregate health wrapper reported `newline=0` for Constitution, pipeline, shell, report, and checkpoint even though UTF-8/provenance/fence/whitespace/conflict relations passed. This run is not credited; the FSM returned to `REVISE` before any follow-on diagnostic.
- diagnostic correction: a direct wrapper captured the terminal byte before decoding and returned `last_byte=10`, `newline=1`, and `utf8=1` for all five files. Perl `Encode::decode` had consumed the prior wrapper's raw scalar before its last-byte predicate. No file-ending edit was required; all bounded gates restart below.

### Restarted bounded SELF-CHECK and resubmission identities

- frozen substantive artifacts: Constitution `2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`, SpecCard pipeline `6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`, and shell `158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; mismatches against the admitted R15 submission `0`;
- report navigation relation: one path/shared-state row, all `10` required historical-snapshot fragments present, and old unqualified four-path wording occurrences `0`;
- stable evidence gates: canonical hashes `9/9`; JSON schema values `5/5`; SpecCard registry `43` sequential rows and `129/129` pending markers; empty template `34` marker-only rows; semantic bodies `0`; loss guards `37/37`;
- controlled-word guards: Constitution/pipeline/shell remain `163` matching lines / `205` occurrences; control-commit additions remain `5` hit-bearing lines / `9` previously dispositioned occurrences, with no new use from this correction;
- clean-room guards: prohibited processor-name, URL, task-inventory, and non-text newly opened-fence hits all `0`;
- file health: all five owned files are valid UTF-8 with provenance/status, terminal byte `10`, balanced fences, trailing whitespace `0`, and conflict markers `0`;
- scope: only `reports/FIX_APPLICATION.md` and this checkpoint differ from their admitted R15 submission hashes in this bounded turn; Constitution, pipeline, and shell do not. Scoped and whole-worktree `git diff --check` pass; untracked paths `0`;
- path observation at this bounded gate: `HEAD=origin/main=7cffe6af7b363a5f53f4322bf1f0eb703c4fb424`, branch `main`; the seven concurrent dirty names were observed by status only and no concurrent body was opened. This observation is itself time-scoped to this check and does not revise the report's earlier four-path snapshot.

Final content identities before this transition:

- `2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe  constitution/MONKEYBEE_CONSTITUTION_v8.md`
- `6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1  constitution/SPECCARD_PIPELINE.md`
- `158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315  shell/MONKEYBEE_SHELL_CORPUS_v8.md`
- `06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602  reports/FIX_APPLICATION.md`
- sorted four-artifact manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- pre-transition checkpoint SHA-256: `58bd473b4d910d08e6b0e35869ba9d1cfe7910931b55dc42816596a41d80732a`. The post-transition checkpoint hash is computed after this append-only mutation and reported externally; embedding that digest in its own bytes would be self-referential.

Terminal phase is `SUBMIT-FOR-REVIEW`. No finding disposition, grade, loss guard, substantive artifact, reviewer route, R16 binding, commit, push, or external action changed.

## R51 follow-up sub-FSM — run record

- control gate: `HEAD=origin/main=cf932eabc47dca8d42b0286995d670a94299f590`, branch `main`;
- submitted artifacts:
  - Constitution: `2009` lines / `187151` bytes / `2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`;
  - SpecCard pipeline: `204` lines / `19387` bytes / `6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`;
  - shell: `6645` lines / `457434` bytes / `158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`;
  - fix report: `405` lines / `66729` bytes / `06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602`;
- bytewise-sorted four-artifact manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- admitted pre-follow-up checkpoint: `554` lines / `110948` bytes / `48951eb4e2e2c470fc7b9da1af26dd8d0cf948d8cf586eda66e2dae97a69b408`;
- write boundary: only the four submitted artifacts and this checkpoint, always through structured `apply_patch`. Concurrent root/G7 paths remain excluded and are neither cleanliness preconditions nor repair targets;
- intake distinction: the human routes R51 as `CHAIN-QUALIFIED`, marginal-only `YES`, with `A=0/B=0/C=0`. This is packet metadata, not an allegation disposition, and no graded allegation exists to accept, narrow, reject, or regrade;
- R17 remains gated and out of scope. No allocation, binding, dispatch, contact, preparation, review route, commit, push, fetch, web action, scratch file, or external action is authorized;
- ingest credit boundary: only control identities and the human-routed packet shape are admitted here. Committed R51 evidence, corrected transcript-bound times, six disclosed variances, G7 recovery, and the informational §34.4/§34.9 candidate remain unread and unassessed until `DRAFT` is persisted.

### Sequencing correction and zero-credit boundary

- root correction: `DRAFT` was required before substantive R51 evidence analysis, not merely before disposition;
- zero-credit reads: the committed Round marker-only search; the committed blob line/byte count; the display of committed lines `5198–5385`; and every conclusion or recollection derived from those reads;
- zero-credit probes: the attempted sealed-control slice, pre-packet prefix, packet slice line/byte count, packet-slice hash, and prefix-plus-packet hash. Their outputs are diagnostic history only and establish no identity, boundary, variance, timestamp, finding count, marginality, or candidate premise;
- interrupted-turn boundary: the interrupted turn supplies no evidence;
- mutation audit: before this `DRAFT` transition, no submitted artifact and no R51 premise disposition changed. The only repository mutation was the required `INGEST` checkpoint write through structured `apply_patch`;
- restart law: every R51 evidence byte needed for this task must now be reread from disk under `DRAFT`. No zero-credit byte or derived value may be carried forward.

### DRAFT reread, mechanical admission, and recovery assessment

- credited committed evidence boundary: marker search and newline-preserving rereads of `gauntlet/ROUND_LOG.md` at control commit `cf932eabc47dca8d42b0286995d670a94299f590`, lines `5198–5309` (R51 control), `5310–5337` (packet), `5338–5365` (G7 recovery), and `5366–5383` (timestamp correction) only. The live Round Log and every unrelated Round body remained unopened;
- reread diagnostic: the first DRAFT aggregate range display collapsed line separators in the tool rendering and receives no credit for line-sensitive relations. Four exact newline-preserving bounded rereads replaced it;
- current committed marker coordinates: control heading byte `959015`, final anchor byte `968865`, packet heading byte `968950`, G7-recovery heading byte `976884`, and timestamp-correction heading byte `983360`;
- control identity: the exact marker-bounded control bytes `[959015,968949)` are `9,934` bytes and hash to `576e7a3542f2f3a3d8c186b9d312e67caf55d18be580d131aa445df10a05bc26`, reproducing the committed recovery's control root independently at its shifted current-blob coordinate;
- packet identity: exact heading-through-termination bytes `[968950,976882)` are `7,932` bytes and hash to `ffe00d970ec926f0385a86a2c5957c0d283ddb7e488b1fa616eba1ef2986ab64`. The file-line span `5310–5337` is `28` lines / `7,934` bytes because it also carries the two separator line feeds outside the hashed heading-through-termination body;
- packet schema: all `12` required packet fields are present; allegation-ID count `0`; `Round verdict` count `1`; termination count `1`; counts remain `A=0/B=0/C=0`; marginal-only remains `YES`. These are reviewer/filed values and are not promoted into new findings or campaign claims;
- variance-counter diagnostic: the first variance wrapper used a full-date sentinel for the write-end local time even though the committed correction abbreviates that second endpoint after “through”; the rerun exposed the missing literal. Replacing only the sentinel with exact local text `12:08:38.386+02:00` reproduced five numbered variances plus the timestamp variance, with all ten required fragments present;
- no-allegation disposition: R51 has no graded allegation to accept, narrow, reject, regrade, or repair. `CHAIN-QUALIFIED` classifies the filing chain; it supplies no premise for an artifact edit. The four submitted artifacts remain frozen.

#### Six disclosed variances — premise-first owner record

1. **Structured Edit surface:** `VARIANCE-CONFIRMED / NON-DISQUALIFYING-ON-COMMITTED-RECOVERY`. The literal named surface differed, while the committed recovery and byte identities support one anchored structured append with the prior prefix preserved. No grade or artifact premise follows.
2. **Reviewer did not recompute the sealed control range:** `VARIANCE-CONFIRMED / NON-DISQUALIFYING`. The blind boundary explains the refusal; G1's independent marker-bounded recomputation above matches the full control root.
3. **Read-only compound Bash:** `RECOVERY-ATTESTED / NO-CONTRARY-LOCAL-EVIDENCE`. The committed G7 recovery binds all three calls after the mandatory start reads and classifies them as read-only. Raw transcript inspection is outside this owner's permitted evidence boundary, so this record does not claim an independent transcript replay.
4. **`FIX_APPLICATION` coordinate `1–406` versus `405` content lines:** `DISPLAY-COORDINATE-NONDEFECT`. Disk reproduces `405` newline-terminated lines, `66,729` bytes, final byte `0x0a`, and the admitted report hash; the empty EOF display coordinate adds no content line.
5. **Constitution wildcard:** `EXACT-TWO-FILE-NONDEFECT`. The committed `constitution/` tree contains only the Constitution and SpecCard pipeline paths, so the wildcard neither under- nor over-covered that directory.
6. **Timestamp field:** `CORRECTION-ADMITTED / QUALIFICATION-RETAINED / NOT-PROTOCOL-PRISTINE`. The packet's authored values are superseded for measured timing: activation `2026-07-15T11:50:35.675+02:00`; Tool 1 `11:52:01.898+02:00`; successful structured write `2026-07-15T12:08:38.262+02:00` through `12:08:38.386+02:00`; terminal text `12:08:40.150+02:00`. The ordering is coherent, the packet bytes/hash remain unchanged, and no finding, grade, allocation, convergence, or artifact conclusion changes.

#### Informational §34.4 / §34.9 candidate disposition

- status: `INFORMATIONAL-PREMISE-SUPPORTED-IN-NARROW-FORM / NO-AUTO-REPAIR / HUMAN-RATIFICATION-REQUIRED`;
- local premise: Constitution §34.4, a kernel section, directly cites shell §0.3 planning gates and §24.13's generated register but does not state the explicit versioned-dependency/kernel-touch law that §34.9 states for shell §27, §27.1, and §27.4. The textual dependency-binding asymmetry therefore exists;
- limiting evidence: Work Order §1 places §24.12–§24.16 in the shell reference corpus and expressly permits cross-home citations while requiring one normative home. No local law found in the bounded sections requires every kernel-to-shell citation to repeat §34.9's versioned-dependency mechanism. Section 34.4 also requires re-observation at every gate, so silently freezing that live register relation could change rather than merely clarify its operation;
- consequence: the evidence supports a dependency-governance question, not a demonstrated PDF-normative loss, filed R51 allegation, grade, or presently authorized artifact defect. No artifact edit qualifies merely to mirror §34.9 wording;
- human-ratification need: a human must decide whether changes to shell §0.3/§24.13 should be declared kernel touches for §34.4, and if so define the version identity, migration/claim-lapse effect, and change-control boundary. Until that decision, preserve current bytes and record the question only here;
- R17 boundary: this disposition satisfies only G1's premise-first handoff obligation. It does not bind, allocate, dispatch, contact, prepare, or authorize R17.

Artifact-repair count under R51: `0`. Finding-disposition count under R51: `0`. Informational candidate dispositions: `1`, non-finding and human-ratification gated.

### R51 SELF-CHECK failure 1

- failed relation: phase/navigation truth. The top-level live phase was `SELF-CHECK`, but the unqualified section heading `R51 follow-up sub-FSM — INGEST` could make a fresh owner treat the initial phase as current despite later transitions;
- diagnostic precursor: the first navigation wrapper omitted UTF-8 source semantics and matched zero Unicode headings. It receives no evidence credit. The corrected UTF-8-aware wrapper found one heading, label `INGEST`, and one conflict;
- correction boundary: rename only the R51 run-record heading so it does not assert a live phase. Preserve every transition row and all R51 evidence/disposition prose. No artifact mutation is authorized;
- restart law: after the bounded correction, persist `REVISE -> SELF-CHECK` and rerun the entire terminal suite from disk. The pre-failure head read and navigation probes are not carried forward.
- correction applied: the heading is now `R51 follow-up sub-FSM — run record`; no transition row, evidence record, disposition, or artifact changed.

### Restarted terminal SELF-CHECK result

- control race bracket: start and end both reproduce `HEAD=origin/main=cf932eabc47dca8d42b0286995d670a94299f590`, branch `main`; no raced result was credited;
- submitted artifact tuples: Constitution `2009/187151/2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`; pipeline `204/19387/6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`; shell `6645/457434/158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; report `405/66729/06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602`; mismatches `0`;
- bytewise-sorted four-artifact manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- R51 evidence: control `9,934` bytes / `576e7a3542f2f3a3d8c186b9d312e67caf55d18be580d131aa445df10a05bc26`; packet `7,932` bytes / `ffe00d970ec926f0385a86a2c5957c0d283ddb7e488b1fa616eba1ef2986ab64`; allegation IDs `0`; verdicts/terminations `1/1`; numbered-plus-timestamp variances `5+1=6`;
- navigation: one phase-neutral R51 run-record heading, zero R51 phase-like headings, and follow-up sequence `INGEST -> DRAFT -> SELF-CHECK -> REVISE -> SELF-CHECK` before this submission; zero artifact repairs and one informational non-finding disposition;
- file health: all five owned Markdown files pass strict UTF-8, terminal LF, CR `0`, trailing whitespace `0`, conflict markers `0`, and balanced fences;
- clean-room/claim guard: R51-added lines have controlled-word, prohibited-processor-name, URL, task-inventory, and new-fence hits `0`. Constitution/pipeline/shell retain their prior `163` matching-line / `205` occurrence enforcement baseline without a new use;
- SpecCard guard: `43` sequential registry rows, `129/129` markers, `34` empty-template rows, semantic bodies `0`;
- path scope: only `ledger/owners/G1_STATE.md` differs among the five writable paths; staged paths `0`; scoped `git diff --check` passes. Concurrent excluded paths were not opened and whole-worktree cleanliness was not required;
- pre-submission checkpoint: `622` lines / `122602` bytes / `6bceb9cfa8dc3a0f9f1f9bda36d080b6f12c8bb6068f17339c0046e0d0389a77`.

Submission boundary: R51 remains `CHAIN-QUALIFIED`, `A=0/B=0/C=0`, marginal-only `YES`, with no finding disposition or repair. The informational dependency candidate remains human-ratification gated. This phase authorizes only final state/hash/navigation verification before `DONE/HANDOFF`; it does not authorize reviewer contact or R17 work.

### R51 terminal root handoff — 2026-07-15T13:19:58+02:00

- transition: `SUBMIT-FOR-REVIEW -> DONE/HANDOFF`, persisted before terminal identity verification;
- pre-transition checkpoint: `638` lines / `125491` bytes / `a3736e7e50685b47a4b71ce41c2909ea4fb43e31fe7d4bd68c8f36c96d2ad2ba`;
- pre-transition control bracket: branch `main`; `HEAD=origin/main=cf932eabc47dca8d42b0286995d670a94299f590`;
- artifact result: all four submitted artifacts remain byte-identical to the admitted R51 inputs; their sorted manifest remains `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- arithmetic: R51 graded allegations `0` (`A=0/B=0/C=0`), finding dispositions `0`, artifact repairs `0`, informational candidate dispositions `1`; the candidate remains non-finding and human-ratification gated;
- diagnostic disclosure: the first post-submission delta wrapper emitted uninitialized-value warnings and blank numeric fields. It is zero-credit. After the mandatory post-compaction AGENTS.md reread, the rerun initialized every counter and reproduced `85` added checkpoint lines with controlled-word, prohibited-name, URL, task-inventory, and fence hits each `0`;
- terminal boundary: no R17 allocation, binding, dispatch, contact, preparation, or reviewer action; no commit, push, fetch, web action, external action, or submitted-artifact edit. Root receives the idle handoff after read-only terminal verification.

### R51 terminal diagnostic failure 2 — 2026-07-15T13:22:08+02:00

- failed relation: a terminal wrapper expected the recorded `163` matching lines / `205` occurrences after scanning Constitution, pipeline, shell, and report together; it returned `177/238` and exited nonzero;
- cause: the persisted baseline is scoped to Constitution/pipeline/shell. The unchanged report was outside that aggregate but was mistakenly added to the wrapper input;
- evidence credit: none. Neither the failed aggregate nor any of its term counts is a pass;
- defect boundary: no submitted artifact changed, and this failed expectation establishes no artifact or controlled-vocabulary defect;
- correction boundary: rerun the recorded baseline over Constitution/pipeline/shell, retain the exact report identity as its separate no-change proof, rerun the initialized added-line guard across the checkpoint delta, and restart every bounded terminal gate before resubmission.

### R51 terminal SELF-CHECK failure 3 — 2026-07-15T13:23:19+02:00

- failed relation: the combined control/packet wrapper exited nonzero after its packet field-count assertions returned verdicts `0` and terminations `0`;
- reproduced but uncredited output: control bytes/hash and packet bytes/hash matched their admitted values, and packet allegation-ID count was `0`. Because the wrapper failed, none of those same-wrapper results is carried into the restarted suite;
- cause boundary: the verifier guessed bold-field and bare-termination line syntax instead of matching the sealed packet's actual formatting;
- evidence and artifact boundary: no packet, submitted artifact, finding disposition, grade, or informational disposition changed. The failed formatting assumption establishes no evidence defect;
- correction boundary: under `REVISE`, inspect only the already permitted committed packet range to obtain its exact verdict/termination syntax. Persist `SELF-CHECK` before rerunning the entire bounded terminal suite with literal syntax and no guessed format.
- correction applied: the permitted sealed-range inspection reproduced exactly one `### Round verdict` heading and one `- termination state: TERMINATED` field. The restarted verifier matches those literal forms; no repository artifact was repaired.

### R51 terminal SELF-CHECK failure 4 — 2026-07-15T13:24:43+02:00

- failed relation: the restarted Constitution/pipeline/shell vocabulary wrapper returned `133` matching lines / `168` occurrences and exited nonzero against the persisted `163/205` baseline;
- cause: the wrapper added whole-word boundaries. The persisted enforcement baseline counts case-insensitive controlled-token substrings, including inflected forms, and therefore has a different measured relation;
- evidence credit: none. The failed wrapper and every earlier result in that restart are discarded;
- defect boundary: file scope was corrected, all submitted hashes remained admitted, and no artifact or vocabulary defect is established by changing the matcher semantics;
- correction boundary: use the persisted case-insensitive substring matcher over Constitution/pipeline/shell, keep the report as a separate hash identity, and restart all bounded gates after the next `SELF-CHECK` transition.
- correction adopted: root independently confirmed that exact matcher scope and semantics. The next restart uses it literally and introduces no new diagnostic variant.

### R51 restarted terminal SELF-CHECK — 2026-07-15T13:26:33+02:00

- control bracket: start and end both reproduce branch `main` and `HEAD=origin/main=cf932eabc47dca8d42b0286995d670a94299f590`;
- submitted identities: Constitution `2009/187151/2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`; pipeline `204/19387/6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`; shell `6645/457434/158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; report `405/66729/06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602`;
- sorted four-artifact manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`; artifact mismatches `0`;
- sealed R51 identities: control `9,934` bytes / `576e7a3542f2f3a3d8c186b9d312e67caf55d18be580d131aa445df10a05bc26`; packet `7,932` bytes / `ffe00d970ec926f0385a86a2c5957c0d283ddb7e488b1fa616eba1ef2986ab64`; allegation IDs `0`; literal verdict/termination counts `1/1`;
- phase/navigation: exactly one live phase surface, one phase-neutral R51 run-record heading, zero phase-like R51 headings, and the append-only transition sequence through this submission is nonconflicting;
- hygiene: all five owned Markdown files are strict UTF-8 with terminal LF; CR, trailing-whitespace, and conflict-marker counts are `0`; fence counts are even at `20/0/60/0/4`;
- controlled-vocabulary relation: the exact original case-insensitive substring matcher over Constitution/pipeline/shell reproduces `163` matching lines / `205` occurrences and its recorded per-token partition. The report remains separately proven by its exact hash;
- clean-room delta: the initialized checkpoint-delta scan covers `129` added lines and returns controlled-token, prohibited-name, URL, task-inventory, and new-fence hits each `0`;
- path/diff: only `ledger/owners/G1_STATE.md` differs among the five writable paths; staged paths `0`; scoped `git diff --check` passes. Whole-worktree cleanliness is not a gate and excluded concurrent bodies remain unopened;
- pre-submission checkpoint: `681` lines / `132438` bytes / `c8e1a8f9097cda5d694a222b8ad1bda3459310cef655a906ab79878035756f7d`;
- result: R51 remains `CHAIN-QUALIFIED`, marginal-only `YES`, `A=0/B=0/C=0`, with finding dispositions `0`, artifact repairs `0`, and one informational human-ratification question. Failures 2–4 and the warning-producing scan remain explicit and zero-credit.

### R51 final DONE/HANDOFF — 2026-07-15T13:27:42+02:00

- transition: `SUBMIT-FOR-REVIEW -> DONE/HANDOFF`, persisted before final read-only hash reporting;
- pre-transition checkpoint: `696` lines / `135251` bytes / `66e03ca101bbbabaee9c6c9c198905b710cf02b50c23c41adf8afde18a12e19c`;
- post-submission guard: branch/control identities stable; four artifact tuples and manifest exact; one live submission phase and phase-neutral run-record navigation; all five files healthy; only this checkpoint changed among writable paths; staged paths `0`; scoped diff health passes;
- initialized delta guard: `143` added checkpoint lines before this transition, with controlled-token, prohibited-name, URL, task-inventory, and fence hits each `0`;
- terminal arithmetic: graded allegations `0` (`A=0/B=0/C=0`), finding dispositions `0`, artifact repairs `0`, informational candidate dispositions `1`; human ratification remains required for that non-finding question;
- terminal boundary: all four submitted artifacts remain byte-identical. No reviewer action, R17 allocation/binding/dispatch/contact/preparation, commit, push, fetch, web action, external action, or excluded-body read occurred. Root receives the idle handoff.

## Overnight scope-reduction hardening record

### Submission reopening — 2026-07-16T02:01:50+02:00

- disk-truth reconciliation: the live checkpoint read at the start of this route was `DONE/HANDOFF`, although the human described the current state as `SUBMIT-FOR-REVIEW`. The prior terminal record is preserved. This explicit human-routed transition reopens it without retroactively changing any earlier phase or result;
- human-supplied scope control: the overnight accepted-review target is now approximately `20`; root's bounded landing is `22` accepted reviews and preserves the `12`-accepted-C1 floor. G1 does not inspect root ledgers or use these control counts as substantive validation;
- omitted-review record: R17 and R18 are cut from overnight execution and remain `UNREVIEWED-OMISSION`. No packet, lens application, finding, grade, convergence credit, pass state, or substantive coverage evidence exists for either round in this owner record;
- disclosure route: the R17/R18 omission and any decision about its acceptability remain pending `MORNING_REPORT` ratification/disclosure outside G1's writable scope. G1 neither writes nor routes that report;
- hardening law: the cut supplies no premise for an artifact change and does not validate any existing artifact. `REVISE` must independently harden the four submitted artifacts against already-admitted owner contracts and review dispositions;
- admitted control: branch `main`; `HEAD=origin/main=6e6771819b4463d11b9d69dce71ef3d23e847da7`; the five owned paths were clean at intake;
- admitted artifacts: Constitution `2009/187151/2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`; pipeline `204/19387/6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`; shell `6645/457434/158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; report `405/66729/06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602`;
- admitted sorted manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- admitted checkpoint: `706` lines / `136748` bytes / `042cd6a14142994db125553fe4eefb2563055ef5f34f65ac16f401201a6f9318`;
- next transition: persist `SUBMIT-FOR-REVIEW -> REVISE` before any hardening read, disposition, or artifact decision. No artifact has changed in this reopening.

### Mandatory REVISE entry — 2026-07-16T02:02:17+02:00

- authority: live human scope override; AGENTS.md through EOF; Owner Marching Orders shared contract and G1 section; current disk checkpoint and its already-admitted disposition/loss-guard records;
- artifact boundary: Constitution, SpecCard pipeline, shell, and fix report may change only if fresh local evidence establishes a bounded defect. State-only closure is preferred when every admitted relation remains satisfied;
- hardening boundary: test disposition traceability, owner-contract homes, identity/lifecycle and refusal/no-claim guards, feature/loss preservation, pending-source registry and empty-slot law, report evidence limits, controlled vocabulary, clean-room, formatting, path scope, and the sorted manifest;
- omission boundary: R17 and R18 have no review evidence. They may not be represented as passed, covered, graded, converged, or equivalent to an applied lens. Their only permitted disposition here is explicit unreviewed omission pending morning ratification/disclosure;
- unavailable human inputs: final morning acceptance of the two omitted rounds; any ratification of the informational Constitution §34.4/§34.9 dependency-binding question; and pending D1/D2/D3 or other recorded human choices remain unavailable. No resolution will be invented;
- action boundary: no round or convergence file, root ledger, orchestration state, other owner path, canonical input, external system, or excluded body may be opened or changed. No reviewer or round is routed;
- next transition condition: persist the fresh hardening evidence and artifact-repair decision under `REVISE`; then reproduce the final stable gate and enter `DONE/HANDOFF` under the governing shared contract and live route.

### REVISE hardening evidence and repair decision — 2026-07-16T02:06:34+02:00

- bounded reads: AGENTS.md through EOF; Owner Marching Orders shared/G1 contract; current checkpoint; report §§11, 12, 12.1, 15, and 16; the R51 non-finding/informational record; the full 204-line SpecCard pipeline; exact owned Constitution/shell disposition homes and schema-example loci. No round, convergence, canonical, root-ledger, orchestration, other-owner, or external body was opened;
- report traceability: R13 rows `5` with Grade-B nonpromotion `2`; R14 rows `19` with Grade-B/C nonpromotion `14`; R15 rows `21` with `QUALIFY=17`, `QUALIFY-NARROW=4`, and Grade-B nonpromotion `7`. Exact monotone ID sets pass; report mentions of R17/R18 are `0/0`;
- owner-contract homes: `12` required Constitution/shell headings occur once at their expected homes; seven Constitution and seven shell identity/lifecycle/refusal/no-claim/loss-guard sentinels pass. The shell retains `13` pending D1/D2 identity markers. The §34.4/§34.9 dependency question remains informational and human-ratification gated rather than silently repaired;
- SpecCard boundary: registry rows `43`, exact sequence `001–043`, marker cells `129/129`, bad rows `0`; empty-template rows `34`, all exact pending-source markers; semantic bodies `0`; slot 043 remains absent from caller-credential decryption;
- schema examples: exactly five JSON values decode; common identity/outcome/coverage/diagnostic/evidence/no-claim envelopes `5/5`; one-way backlinks and telemetry exclusions `5/5`; B.2 retains `22.1/18432122` only in the run observation; partial-source, apply-admission, processor-projection, and checker-isolation/trial/lineage guards pass;
- JSON diagnostic failures: the first wrapper passed decoded characters to an octet-only helper and stopped on a non-ASCII value; the second forced every observation into a nested telemetry object even though B.2 intentionally uses direct observation fields; the third guessed `processor_results` where the owned B.4 schema uses `processors`. All three are zero-credit verifier errors. The corrected character-aware, schema-literal wrapper produced the result above;
- navigation correction: the initial REVISE-entry condition named an extra `SELF-CHECK` transition inconsistent with the Owner Marching Orders shared `REVISE -> DONE` contract and this live human route. It was corrected under `REVISE` before terminal work; no artifact, disposition, gate result, or omission record changed;
- controlled-vocabulary gate: the exact original case-insensitive substring matcher over Constitution/pipeline/shell reproduces `163` matching lines / `205` occurrences and the recorded per-token partition; the report remains separately identified by its hash. The current checkpoint delta before this evidence append had `29` added lines and zero controlled-token, prohibited-name, URL, task-inventory, or fence hits;
- cut-round boundary: all four submitted artifacts contain `0` R17 mentions and `0` R18 mentions. This absence is not lens coverage; it corroborates only that no cut-round artifact assertion was introduced. No packet, finding, grade, pass state, convergence credit, or substitute lens evidence exists here for either round;
- exact artifacts: Constitution `2009/187151/2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`; pipeline `204/19387/6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`; shell `6645/457434/158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; report `405/66729/06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602`;
- sorted four-artifact manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- file health: all five owned files are strict UTF-8 with terminal LF; CR, trailing-whitespace, and conflict-marker counts `0`; fences balanced at `20/0/60/0/4`; scoped diff health passes;
- path/control: branch `main`; `HEAD=origin/main=6e6771819b4463d11b9d69dce71ef3d23e847da7`; only this checkpoint differs among writable paths; staged paths `0`; pre-evidence checkpoint `733` lines / `141463` bytes / `4beb4d76f6956768b7c1ec691ef831e374a2f2e992d16a692cc21f02db34af80`;
- repair decision: artifact corrections `0`. Exact identity plus fresh disposition, contract-home, schema, pending-source, loss-guard, controlled-vocabulary, clean-room, formatting, and scope checks establish no authorized defect. The human's review-count reduction is not used in this decision;
- unavailable decisions: morning ratification of the R17/R18 omission; the informational §34.4/§34.9 dependency choice; and recorded D1/D2/D3 or other pending human choices remain unresolved. State-only closure neither decides nor strengthens them;
- action result: no artifact edit, external action, reviewer/round route, commit, push, fetch, staging, publication, purchase, contact, measurement, comparison execution, or out-of-scope write occurred;
- next transition: after reproducing this state append's integrity and final manifest, persist `REVISE -> DONE/HANDOFF` before final external hash reporting.

### DONE/HANDOFF scope-reduction closure — 2026-07-16T02:07:48+02:00

- transition: `REVISE -> DONE/HANDOFF`, persisted before final read-only disk identity reporting;
- evidence that exists: exact R13/R14/R15 disposition tables and loss guards; the R51 zero-allegation/non-finding record and informational human-ratification question; exact four-artifact hashes; report traceability; owner-contract homes; five decoded generated examples; pending-source registry/slot proof; controlled-vocabulary, clean-room, hygiene, scope, and manifest gates;
- evidence that does not exist: no R17 packet, applied lens, finding, grade, pass state, coverage claim, convergence credit, or review-derived artifact premise; the same absence applies independently to R18. Neither omission is represented as a successful review or as evidence that its intended lens was covered elsewhere;
- closure authority: the live human reduced overnight execution to an approximate-20 accepted-review target and reported a bounded landing of `22` while preserving the `12`-accepted-C1 floor. That instruction authorizes stopping with disclosed omissions; it does not validate an artifact, premise, grade, lens, or convergence claim;
- closure basis: the mandatory hardening pass independently found artifact repairs `0` against the evidence that actually exists. G1 can close because the human removed R17/R18 from overnight scope, not because those rounds passed or because their missing evidence was inferred;
- exact frozen artifacts: Constitution `2009/187151/2d3828110ae9d525003a19ee5340141a1f347be44be275520fe4c8253be789fe`; pipeline `204/19387/6a15a6ac957bfe3f700acc383313c8945770578b19a1f549bd41816169a0bfb1`; shell `6645/457434/158679c21172682e33c0e30ca6c670bf079602394170d5478000e5e3b8032315`; report `405/66729/06faddd7deef40524056b86ea192c85a5f0822cc3f5a7768b3d27a991922f602`;
- exact sorted manifest: `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`;
- pre-transition checkpoint: `753` lines / `146630` bytes / `c36a62b22d14e925ce4b10727d0474d9c68bb7eb6504bdc3a0b02717320a7b1c`;
- unresolved ratifications: morning acceptance/disclosure of both omitted rounds, the informational §34.4/§34.9 dependency question, and all recorded pending human identity/serialization choices remain unresolved;
- disclosure boundary: `MORNING_REPORT` must identify R17 and R18 as unreviewed omissions and obtain or record the human disposition. G1 has not written, routed, or prefilled that out-of-scope artifact;
- final action boundary: only this checkpoint changed. No substantive artifact, round/convergence document, root ledger, orchestration state, other-owner file, canonical input, source, or external system changed; no review/round was routed;
- terminal work: compute the post-transition checkpoint identity and reverify phase, path, hygiene, delta, artifact, and manifest facts read-only, then stop idle.

## R14 replacement-owner intake — persisted before artifact edits

- replacement authority: the human routed G1 replacement ownership and root-verified this process as `gpt-5.6-sol`, reasoning effort `ultra`, live service tier `priority`, fast mode. This identifies the replacement process; it does not establish any campaign or finding premise.
- admitted orchestration baseline: pushed commit `353d72e0d52b831415af7035d593b85991560760`, locally checked out on `main` and tracking `origin/main`.
- pre-edit four-file components:
  - `6c68a7881be552a74d0105c8e9e46b87c57b64c64a69e69eda7e215f628fcff6  constitution/MONKEYBEE_CONSTITUTION_v8.md`
  - `ceac345d4d580efc6edee81fb7052fc0ffd3ebc97df97b3c24e271ddf8a6a8b3  constitution/SPECCARD_PIPELINE.md`
  - `2ddcfb8d6267c3f2e4b3cff62423aa3eb440f719bca428151cb130a34420d245  shell/MONKEYBEE_SHELL_CORPUS_v8.md`
  - `70dc3710e607897f1d86841d469bf57c97302a298f3c5acfaa67894db317be1d  reports/FIX_APPLICATION.md`
- pre-edit sorted-manifest SHA-256: `dd86aaf319ecc87d0526d90b56b1993e8023925eeec4467ffd7807f89f92efb4`; method is ordinary `sha256sum` lines, bytewise sorted under `LC_ALL=C`, final newline included, then SHA-256.
- pre-edit checkpoint SHA-256: `4861043c70c7de0462739a2d8a9ce83806bf66f0c62b98066044c94d33b0435b`.
- admitted Round Log SHA-256: `26b17afbdb47751d22f082d5f759b489de90c3b34c5e72966c3e6906e50680ab`.
- admitted reviewer suffix: shifted byte range `[331012,387116)`, length `56104`, SHA-256 `4567275fbaba4e3e04d433ec4757897ed6ae4aee183ada2c552a28785794a122`.
- read boundary: `AGENTS.md`; `INDEX.md`; `OVERNIGHT_GOAL.md`; the owner marching orders; this checkpoint; the Charter, Work Order, and Audit Ledger; all four owned artifacts; and only Round Log lines `1815-2081`. No prior-round finding body or unrelated owner disposition was read.
- read diagnostics: the first aggregate read of the orientation chain exceeded the display bound and was replaced by per-file bounded full reads. Shell reads `3601-3900` and `5101-5400` exceeded the display bound and were each replaced by two contiguous bounded reads with no omitted interval. These were display failures, not evidence passes.
- worktree coexistence: pre-existing modifications in `ledger/owners/G3_STATE.md` and `plans/CYCLE_1_DELTA_PLAN.md` were observed and remain outside G1 scope; they will not be touched, stashed, reverted, or used as G1 evidence.
- reviewer-process disposition: `METHOD-NONCOMPLIANT` / `PROCESS-QUARANTINED`. The reviewer disclosed an unauthorized outside-repository scratch-manifest write, and the final `TERMINATED.` line varies from the exact standalone `TERMINATED` token. Mechanical validation, process death, and bounded routing do not prove or refute any substantive premise.
- packet handling law: R14 contains exactly 19 unconfirmed allegations: `A=5`, `B=10`, `C=4`. Filed grades and loss guards are preserved below. A grade is never proof; B and C remain B and C regardless of premise result or wording. No artifact repair is authorized until its local premise qualifies.

| Finding | Filed grade | Routed premise under local test | Preserved loss guard | Intake disposition |
|---|---|---|---|---|
| `R14-A01` | `A` | `OpenReport` allegedly lacks the security-inventory interface required by the C1/Workflow-B gate. | Preserve every existing §10.6 field, the full §9.3 View-G enumeration, §16.8's reachability/effect graph rather than a flat inventory, cross-hypothesis security coverage, and §10.9 availability gating. | `PENDING-LOCAL-PREMISE-TEST`; process quarantine and grade establish nothing. |
| `R14-A02` | `A` | The kernel's Gauntlet-tier obligation allegedly depends on an unbound provisional-shell taxonomy. | Collapse, merge, or drop no tier; preserve G6.1, every §27.1 falsifier family including the newer proof/GPU/replay/agent/recognition/comparison rows, and §27.4 consequence-contract validity law. | `PENDING-LOCAL-PREMISE-TEST`; process quarantine and grade establish nothing. |
| `R14-A03` | `A` | The SpecCard registry allegedly has no empty slot for extension dictionaries/levels or catalog `/Version` override. | Preserve §12.3.1's `declared` / `required_by_features` / `selected_for_interpretation` / `target_for_write` distinction, per-revision computation, and separate ISO/TS profile registration. | `PENDING-LOCAL-PREMISE-TEST`; process quarantine and grade establish nothing. |
| `R14-A04` | `A` | The lossless-filter coverage map allegedly omits the existing `Crypt`/`Identity` routing slot. | Do not merge filter and encryption surfaces; preserve acyclic `mb-filter`/`mb-security` ownership and retain `MB-SC-R0-029` under both standard-handler rows. | `PENDING-LOCAL-PREMISE-TEST`; process quarantine and grade establish nothing. |
| `R14-A05` | `A` | The extraction change-accounting claim allegedly exposes no reproducible disposition for 139 of 142 non-identical source lines. | Any accounting must name each carried requirement, exact output locus, and change authority; count/diff agreement alone is insufficient, and no artifact may be edited merely to shrink the diff. | `PENDING-LOCAL-PREMISE-TEST`; process quarantine and grade establish nothing. |
| `R14-B01` | `B` | `D1R` allegedly falls out of the §10.6/§10.8 replay-relation enumerations. | Do not collapse D1R into D1 or D4; preserve semantic-plan/non-secret-receipt reproducibility without byte identity and the exact deterministic-signature protected-input law. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B02` | `B` | The CMap interpreter allegedly lacks an explicit sandbox tenant and limit dimension. | Preserve the dedicated CMap interpreter, never fold it into Type 4 or general PostScript, and retain predefined-CMap data-pack version identity. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B03` | `B` | Hostile XML/XMP parsing allegedly lacks a general kernel work dimension distinct from static-XFA projection. | Keep XML parse budgets separate from projection-output budgets and preserve their separately charged, transactional publication. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B04` | `B` | `TextReport` allegedly omits order alternatives/type, distinct text classes, and per-span CID/GID evidence. | Preserve all existing fields and never let an order label imply correctness; retain human-review and semantic-quality no-claim boundaries. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B05` | `B` | `RenderReport` allegedly omits effective dialect, resource consumption, and non-operator unsupported/policy-blocked features. | Preserve all existing fields and keep semantic counters distinct from wall-clock cancellation/observation. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B06` | `B` | Constitution §4.4's dated standards identifiers allegedly require `[UNVERIFIED]` treatment under the no-fetch record. | Delete no identifier; preserve the edition/amendment/corrigendum versus association-collection distinction and separate 32001-32004 and 32005 family treatment. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B07` | `B` | The §9.2 table allegedly omits five identity classes minted elsewhere in the kernel. | Preserve all identity-domain boundaries; a capability-contract identity must not become document reality or a derivation-cache key. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B08` | `B` | Cross-reference/revision coverage allegedly omits the existing object-stream decryption-boundary slot. | Retain `MB-SC-R0-031` under both encryption rows and preserve credential/tenant/revision cache partitioning; this is a dependency edge, not a syntax re-home. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B09` | `B` | The normative SpecCard/template shape allegedly lacks fixture, contract, and external-profile impact linkages named in the shell. | Retain a mechanically computable card-to-impact path; deleting forward fields qualifies only with an explicit reverse-index guarantee. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-B10` | `B` | The 43 Rule-13 wording rewrites allegedly have no reproducible feature-preservation interface. | Record before/after obligation by locus without re-editing merely for reconciliation; if scope narrowed, restore the obligation in Rule-13-legal language, not prohibited wording. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `B` under every outcome. |
| `R14-C01` | `C` | B.4's `external_output_artifact_ids` name allegedly conflicts with its all-processor projection. | Preserve the unordered processor-result set, in-record processor/output binding, exact set projection, and absence of positional priority, role, outcome, winner, or majority semantics. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `C` under every outcome. |
| `R14-C02` | `C` | Constitution §11.1 allegedly names a shell-only `ChildContextStorage` construct. | Preserve the entire R2-N4 repair: retained parent/local intersection, no temporary borrow, and the bounded `Sync` diagnostic collector with callback-scoped non-reentrancy. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `C` under every outcome. |
| `R14-C03` | `C` | `OpenReport` allegedly collapses the exact-preservation interval ledger to unexplained bytes. | Preserve independent overlapping interval facets; never replace them with one exclusive enum. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `C` under every outcome. |
| `R14-C04` | `C` | The registry allegedly lacks an empty inventory slot for ISO/TS AES-GCM/integrity variants. | Do not widen C1 read decryption beyond RC4/AESV2/AESV3; preserve credential-bound partial/refusal behavior and separate core/ISO-TS classification. | `PENDING-LOCAL-PREMISE-TEST`; remains filed Grade `C` under every outcome. |

- selected repair files at intake: none. That intake gate is now superseded by the independently persisted premise results below.

## R14 independent premise test — 2026-07-14T21:59:09+02:00

The replacement owner tested all 19 allegations against the human-routed local authority chain and exact owned artifacts. Reviewer wording, mechanical validation, filed grade, and quarantined process behavior supplied no premise evidence. Searches were limited to the admitted repository sources: Charter C0/C1 and per-cycle law; Work Order §§1–3 and §7; the Audit chain; Constitution §§4, 9–11, and 34.9; Rev 7's corresponding stable sections and the bounded shell-domain loci they leave authoritative for planning; the pipeline; Appendix B; and the fix map. No web, fetch, prohibited processor source/documentation, measurement, comparison execution, or unavailable-source reconstruction occurred.

| Finding | Independent local result | Smallest loss-preserving repair | Grade handling |
|---|---|---|---|
| `R14-A01` | **QUALIFIES.** Charter C1 requires the identity/evidence/report stack for the R0 inspection wedge; Rev 7 §3.0 and Workflow B require encryption, attachments, actions/active content, shadowed/unreachable history, metadata across revisions, and provenance. Constitution §16.8 requires a reachability/effect graph with per-node trigger, target, revision, visibility, host capability, executable class, and sanitization state. Constitution §10.6 `OpenReport` currently has only encryption state, signatures, unexplained bytes, and limits; it has no security/behavior graph or metadata-history field. | Add the §16.8 reachability/effect inventory and metadata-across-revisions obligations to `OpenReport`, scoped by revision/view/hypothesis and coverage/availability, then regenerate B.1. Keep all existing fields and §10.9 availability law. | Remains filed Grade `A`; grade supplied no proof. |
| `R14-A02` | **QUALIFIES IN NARROWED FORM.** Work Order §1 deliberately leaves §§12–28, including §27, in the shell, so moving or duplicating the taxonomy into the kernel would violate the extraction split. Yet Constitution §34.9 makes the assigned tier the definition of done without binding the exact shell taxonomy/falsifier/matrix version on which that term depends. | Preserve the supplied §34.9 text byte-for-byte and append a kernel binding to this version's shell §27 taxonomy, including G0–G7/G6.1, §27.1, and §27.4. Any later taxonomy change becomes a declared kernel touch; no tier is collapsed or copied. | Remains filed Grade `A`; the broader re-home suggestion is rejected. |
| `R14-A03` | **QUALIFIES.** Rev 7 §12.3.1 explicitly includes catalog `/Version` overrides and extension dictionaries/levels in per-revision `EffectiveDialect`; pipeline slots 001–040 contain neither surface. | Add empty slots 041–042 and map them to header/effective-dialect coverage. Every source/semantic/review field stays exactly `PENDING-LICENSED-SOURCE`; no standard meaning is authored. | Remains filed Grade `A`; grade supplied no proof. |
| `R14-A04` | **QUALIFIES.** Slot 029 already owns crypt-filter selection/identity routing, while the lossless-filter coverage row lists only slots 005 and 016–022. The standard-handler rows already retain 029. | Add slot 029 as a dependency of the lossless-filter planning surface without merging filter and encryption ownership. | Remains filed Grade `A`; grade supplied no proof. |
| `R14-A05` | **QUALIFIES.** The report states that all 142 exact-line exceptions are accounted for, but names only the three R13 replacements; it gives neither the 139-source-line set nor a per-locus output and change-authority interface. A fresh local set-difference reproduced 5,266 unique nonblank source lines and 142 exceptions. | Add a partitioned 142-line register: 43 Rule-13 rewrites with before/after obligation by stable locus; 18 extraction/fix exceptions; 78 schema-regeneration lines grouped by B.1–B.5 with every line enumerated; and three R13 replacements. State the exact comparison method and authority for each partition. Do not edit artifacts to reduce the count. | Remains filed Grade `A`; grade supplied no proof. |
| `R14-B01` | **QUALIFIES.** Constitution §11.9 defines D1R separately, but the §10.6 replay bundle and §10.8 replay sentence enumerate D0/D1/D2, D3, D4, and D5 without stating D1R's semantic-plan/non-secret-receipt relation. | Add D1R explicitly at both replay loci, preserving its no-byte-identity and deterministic-signature protected-input limits. | Remains filed Grade `B`; not promoted. |
| `R14-B02` | **QUALIFIES.** Rev 7 §§12.2 and 20.5 require a bounded dedicated CMap interpreter, cycle-safe `usecmap`, aggregate/depth limits, and rights-cleared predefined-CMap data-pack identity. Constitution §11.8 and §11.2 omit that tenant and work dimension. | Add a CMap work-limit row and a narrow §11.8 tenant sentence that cites the dedicated, non-Type-4/non-general-PostScript interpreter and versioned data-pack capability. | Remains filed Grade `B`; not promoted. |
| `R14-B03` | **QUALIFIES.** Shell §16.10 requires hostile XML/XMP budgets and says metadata extraction and XML-derived layout are separately charged and transactionally published. Constitution §11.2 names only static-XFA nodes/layout/output. | Add a distinct hostile XML/XMP parsing work dimension; retain the static-XFA projection row. | Remains filed Grade `B`; not promoted. |
| `R14-B04` | **QUALIFIES.** Rev 7 Workflow C and shell §§20.13–20.15 require four text-view classes, glyph-level CID/GID evidence, visibility, and labeled/ranked reading-order alternatives. Constitution `TextReport` names only a singular reading-order algorithm and lacks those fields. | Add text-view class, per-span CID/GID/evidence/visibility, and reading-order type plus alternatives; preserve all existing fields and no-claim boundaries. | Remains filed Grade `B`; not promoted. |
| `R14-B05` | **QUALIFIES.** Rev 7 Workflow A requires selected revision/hypothesis evidence, effective dialect, resource consumption, warnings/refusals, and unsupported/policy-blocked features. `RenderReport` currently narrows the last category to operators and omits the other facts. | Add those family fields and regenerate B.2 while retaining every existing semantic field and keeping wall-clock observations only in the run envelope. | Remains filed Grade `B`; not promoted. |
| `R14-B06` | **QUALIFIES IN NARROWED FORM.** Constitution §4.4 exactly inherits Rev 7's dated ISO/errata/profile identifier and status assertions. This no-fetch owner has no admitted source that verifies them, and Overnight Goal Rule 8 requires unverifiable facts to be marked `[UNVERIFIED]`. | Preserve the inherited paragraph verbatim and prefix a paragraph-scoped `[UNVERIFIED]` notice stating that no licensed/public standards source was contacted. Delete or reinterpret no identifier. | Remains filed Grade `B`; not promoted. |
| `R14-B07` | **QUALIFIES.** Constitution §9.2 is the typed identity registry but omits `ConsequenceContractId`, `EvaluationProtocolId`, `CompetitorDiscoveryProtocolId`, `CompetitorDiscoveryReportId`, and `SpecCardId`, all minted by §§4 and 10. | Add five rows using only the meanings already stated at those normative homes; do not change identity encoding or ratification. | Remains filed Grade `B`; not promoted. |
| `R14-B08` | **QUALIFIES.** Cross-reference/revision coverage lists slots 006–012, but encrypted object streams cross the existing slot-031 boundary. Both encryption rows already retain slot 031. | Add slot 031 only as a cross-reference/revision dependency edge. | Remains filed Grade `B`; not promoted. |
| `R14-B09` | **QUALIFIES.** Shell §13.9 preserves linked fixtures, implementation contracts, and external profiles as card fields. Constitution §4.3 and the pipeline template omit them; pipeline step 9 supplies only a partial reverse linkage. | Add the three project-linkage fields to Constitution §4.3 and the empty template/review path. Every template value remains exactly `PENDING-LICENSED-SOURCE`; no semantic body appears. | Remains filed Grade `B`; not promoted. |
| `R14-B10` | **QUALIFIES.** The fix report gives aggregate Rule-13 counts and examples but no reproducible locus map for the 43 source lines whose ordinary vocabulary changed. The local exact-line exception set isolates 43 such lines after excluding OPEN-C0/R13/schema changes. | Add a 43-row source-line/stable-home/current-wording/retained-obligation/authority register. Reword no artifact merely to reconcile it. | Remains filed Grade `B`; not promoted. |
| `R14-C01` | **QUALIFIES.** B.4's `external_output_artifact_ids` contains both MonkeyBee and external-subject outputs, so the field name contradicts the exact all-processor projection required by Constitution §10.6. | Rename it `processor_output_artifact_ids` in the normative field law, generated example, and validator. Preserve the unordered identity-keyed bindings, exact set equality, and no positional/outcome semantics. | Remains filed Grade `C`; not promoted. |
| `R14-C02` | **QUALIFIES.** Constitution §11.1 names `ChildContextStorage`, a concrete sketch type defined only in provisional shell Appendix A.2. The kernel law needs lifetime/storage behavior, not that shell type name. | Replace the name with “caller-provided storage that outlives the child context”; retain ownership/borrow alternatives, full-lifetime intersection, shared accounting, and the separate bounded `Sync` collector law. | Remains filed Grade `C`; not promoted. |
| `R14-C03` | **QUALIFIES.** Shell §12.11 requires independent overlapping interval facets for ownership, preservation, explained/unexplained ranges, revision membership, cryptographic coverage, sensitivity, and carry/drop intent. `OpenReport` exposes only unexplained bytes. | Add the exact-preservation interval-facet ledger as an `OpenReport` field and regenerate B.1 with independent facet collections; keep `unexplained bytes` as its own field. | Remains filed Grade `C`; not promoted. |
| `R14-C04` | **QUALIFIES IN NARROWED FORM.** The planning registry has no empty inventory slot for the separately named ISO/TS encryption/integrity family, while §4.4 itself says technical-specification profiles are separate. Nothing in the admitted chain authorizes wider C1 decryption. | Add empty slot 043 and an inventory-only coverage row. Keep caller-credential C1 read decryption exactly RC4/AESV2/AESV3; do not route slot 043 into that row. | Remains filed Grade `C`; not promoted. |

- premise failures requiring conscious non-repair: none. `R14-A02`, `R14-B06`, and `R14-C04` qualify only in the narrowed forms above; their broader implications are expressly rejected.
- selected repair files: `constitution/MONKEYBEE_CONSTITUTION_v8.md`, `constitution/SPECCARD_PIPELINE.md`, `shell/MONKEYBEE_SHELL_CORPUS_v8.md`, and `reports/FIX_APPLICATION.md`; this checkpoint records each partial state. No other path is writable.
- normative homes: Constitution owns §§4, 9–11, and the §34.9 binding; the pipeline owns empty slot/coverage operations; shell Appendix B is a non-normative generated echo; the fix report owns exception and finding traceability.
- diagnostic disclosure before repair: the first unbounded exact-line exception display exceeded its result bound and triggered context compaction; `AGENTS.md` was then reread in full before resumption. A later parallel targeted-search aggregate and a later multi-range canonical aggregate also exceeded their display bounds. Neither aggregate was treated as evidence: the exception set was rerun in four bounded ordinal slices, and all premise-bearing source/artifact ranges were reread in smaller contiguous calls. The reproduced total is 142, with no omitted ordinal.
- loss-guard admission: every filed loss guard remains mandatory. The repair admits no new standard semantics, decryption capability, comparison result, measurement, SpecCard body, ratified identity encoding, or strengthened campaign claim.

## R14 repair checkpoint 1 — 2026-07-14T22:01:41+02:00

- Constitution partial: applied A01/A02, B01–B07, B09, and C01–C03 at their normative homes. The §4.4 inherited paragraph remains verbatim beneath a paragraph-scoped `[UNVERIFIED]` notice; the supplied §34.9 text remains verbatim and the shell-taxonomy binding follows it. Existing report-family fields remain present.
- pipeline partial: added sequential empty slots 041–043; all nine new registry state cells are exactly `PENDING-LICENSED-SOURCE`; added only the A03/A04/B08/C04 dependency edges and the B09 empty linkage fields. Slot 043 is inventory-only and is absent from caller-credential read decryption.
- generated-echo partial: regenerated B.1 for interval/security/metadata obligations, B.2 for render obligations, and B.4's summary name. Existing examples and R13 identity envelopes remain in place.
- pending partial: `reports/FIX_APPLICATION.md` still requires the 19-finding map and the 142-line/43-line traceability interfaces. No R14 self-check result is claimed yet.
- partial diagnostics: `git diff --check` passes for the three content artifacts. The registry-row count is 43. Targeted sentinels find the unverified marker, Gauntlet binding, two new limit dimensions, B.1 additions, and renamed B.4 field. These are patch-presence checks only, not the required SELF-CHECK suite.

## R14 repair checkpoint 2 / SELF-CHECK entry — 2026-07-14T22:04:58+02:00

- `reports/FIX_APPLICATION.md` now contains one disposition per R14 ID, the exact `18 + 43 + 78 + 3 = 142` exception partition, every schema-source line number, three R13 source replacements, and a 43-row Rule-13 before/after obligation register.
- pre-entry checks: four-artifact `git diff --check` passes; the Rule-13 table has exactly 43 data rows; all five Appendix B JSON values decode successfully in memory.
- diagnostic failures: the first combined pre-entry wrapper failed JavaScript parsing before any shell command executed because literal Markdown fence backticks terminated its wrapper string. The replacement smaller commands ran. The first two in-memory JSON-regex attempts found zero blocks due, respectively, to an uninitialized input scalar and over-quoting the newline; neither parsed or validated content. The corrected in-memory decoder found and decoded exactly five JSON values. No failed diagnostic was treated as a pass.
- transition law: artifact repair is closed for this pass. Any SELF-CHECK failure will transition back to `REVISE` and be persisted before a corrective content patch; otherwise the next allowed transition is `SUBMIT-FOR-REVIEW` after the full suite and final hashes.

## R14 SELF-CHECK failure 1 — 2026-07-14T22:06:29+02:00

- failed relation: exact-line exception/register equality. The actual set acquired Rev 7 local lines 1872 and 1999 while the registered set correctly remained the admitted 142-line baseline.
- cause: R14-B04 expanded the source bullet `reading-order algorithm` in place; R14-B01 inserted D1R by replacing the source replay-refusal paragraph. Both obligations were semantically retained, but A05's loss guard requires preserving the byte-identical source line when adjacency suffices.
- authorized correction: restore each source line verbatim and add a separate adjacent R14 sentence/bullet. No registry, schema, grade, or other disposition changes.
- diagnostic history in this check: the first register script also failed to find a Unicode-em-dash heading literal because the command literal and UTF-8 file did not compare under the assumed decoding path. It was replaced by ASCII stable-section anchors. The corrected script then exposed the substantive two-line mismatch above. Neither result was treated as a pass.

## R14 repair checkpoint 3 / SELF-CHECK restart — 2026-07-14T22:07:19+02:00

- Constitution §10.6 again contains the exact source bullet `reading-order algorithm;`, followed by the new order-type/alternatives bullet.
- Constitution §10.6 again contains the exact source replay-refusal paragraph, followed by the new D1R paragraph.
- restarted extraction check passes: source unique nonblank lines `5,266`; unique level-2–4 headings `554`; missing headings `0`; exact-line exceptions `142`; report partitions `18+43+78+3`; duplicate registered source lines `0`; registered/actual set equality `PASS`.
- the suite restarts rather than carrying any pre-failure pass forward.

## Concurrent shared-state event during R14 SELF-CHECK — 2026-07-14T22:11:56+02:00

- `main` and `origin/main` moved concurrently from the admitted R14 baseline `353d72e0d52b831415af7035d593b85991560760` to `4aa7e83d84fd5b1b92925da49b9aa8c069ef7b10`. G1 performed no commit or push.
- commit-to-commit changed paths are `gauntlet/ROUND_LOG.md`, `ledger/ORCHESTRATION_STATE.md`, `ledger/RUN_LEDGER.md`, `ledger/owners/G3_STATE.md`, `ledger/owners/G7_STATE.md`, and `plans/CYCLE_1_DELTA_PLAN.md`. The four G1 artifact blobs are byte-identical between those commits (`git diff --quiet` exit 0), so `353d72e...` remains the admitted and reproducible G1 content baseline.
- concurrent working modifications now also exist in `gauntlet/ROUND_LOG.md`, `ledger/ORCHESTRATION_STATE.md`, and `ledger/RUN_LEDGER.md`. G1 did not read their new content and will not touch, stash, revert, cite, or include it as evidence.
- path/diff checks are anchored to `353d72e...` for the five exclusive G1 paths and separately report current shared status. This is coexistence, not permission to widen scope.
- failed diagnostic: the first path/format Perl check had a missing quote in its UTF-8 open mode and failed compilation. Because the shell invocation contained later read-only commands on new lines without fail-fast control, those commands still exposed the concurrent HEAD/status change. No format/path relation was credited. The corrected suite is rerun below against the admitted baseline.

## R14 final SELF-CHECK and submission boundary — 2026-07-14T22:22:11+02:00

All four owned artifacts were frozen before the component hashes below were computed. `ledger/owners/G1_STATE.md` is not a component of that manifest and is updated afterward to carry this checkpoint. A passing check establishes only its named relation; it does not validate the quarantined reviewer method, promote a filed grade, supply licensed normative meaning, ratify D1/D2/D3, execute a comparison, or strengthen a campaign claim.

### Premise and repair result

- independently tested allegations: `19`, comprising filed `A=5`, `B=10`, `C=4`;
- qualifying dispositions: `19`; outright premise failures: `0`;
- narrowed qualifying premises: `3` — `R14-A02`, `R14-B06`, and `R14-C04`;
- Grade-B and Grade-C promotions: `0`; every filed grade and loss guard remains explicit in the intake and report row;
- exact source exception register: `142 = 18 + 43 + 78 + 3`, with no duplicate, missing, or extra source locus;
- artifact repairs were limited to the four declared content artifacts; this checkpoint is the fifth exclusive writable path.

### Reproduced final suite

| Check | Final result |
|---|---|
| Extraction / feature preservation | Rev 7 unique nonblank lines `5,266`; unique level-2–4 headings `554`; missing headings `0`; exact exceptions `142`; partitions `18+43+78+3`; registered total `142`; duplicates `0`; actual-not-registered `0`; registered-not-actual `0`; `PASS`. |
| Normative homes / §34.9 | Kernel-home and generated-echo sentinels `PASS`; supplied Work Order §34.9 body byte-line match `PASS`; shell taxonomy exactly G0–G7 plus G6.1; Constitution taxonomy/falsifier/matrix binding `PASS`. |
| Schema examples | Fenced JSON values `5`; common typed identity envelopes and one-way backlinks `PASS`; B.1 Open, B.2 Render, B.3 Transform, B.4 processor/output projection, and B.5 checker assertions all `PASS`. |
| SpecCard registry / markers | Registry rows `43`; exact sequence `001–043`; registry markers `129/129`; empty-template rows `21`; link fields and coverage edges `PASS`; caller-credential decryption is not widened; semantic bodies `0`. |
| R14 targeted regressions | Finding rows `19` with `A=5`, `B=10`, `C=4`; A01–A05, B01–B10, and C01–C04 `PASS`; every B/C row remains explicitly not promoted. |
| Canonical inputs | `9/9` hashes in `ledger/CANONICAL_HASHES.md` match. |
| Claim-vocabulary baseline | Content artifacts remain `160` matching lines / `202` occurrences: `best=24`, `complete=113`, `fastest=0`, `mogged=1`, `supremacy=64`. The `114` added content-artifact lines contain `0` claim-vocabulary hits. |
| Clean-room added-line scan | Added content-artifact lines `114`; prohibited-processor-name hits `0`; URL hits `0`. No web/fetch, prohibited source/documentation contact, SpecCard semantic body, comparison/measurement execution, code, build scaffold, Bead/pseudo-Bead, or external action occurred. |
| Formatting / diff | All five G1 files valid UTF-8; provenance/status fields present; fences balanced; trailing whitespace `0`; conflict markers `0`; admitted-baseline and whole-worktree `git diff --check` both exit `0`. |
| Path boundary | Exactly five G1 paths changed against admitted baseline `353d72e0d52b831415af7035d593b85991560760`; untracked files `0`. `HEAD` and `origin/main` are concurrently `4aa7e83d84fd5b1b92925da49b9aa8c069ef7b10`; the four G1 artifact blobs are identical between that commit and the admitted baseline. |

At the final pre-hash status read, five unrelated dirty paths existed: `gauntlet/CONVERGENCE_REPORT.md`, `gauntlet/ROUND_LOG.md`, `ledger/ORCHESTRATION_STATE.md`, `ledger/RUN_LEDGER.md`, and `ledger/owners/G7_STATE.md`. The first three were already dirty at the earlier path checkpoint; the convergence report and G7 checkpoint appeared later. G1 did not read their new content or touch, stash, revert, reconcile, or cite it. The earlier report statement naming three paths is a timestamped earlier observation; this owner checkpoint records the later count without rewriting that history.

### Final frozen four-component hashes

The manifest method is ordinary `sha256sum` lines with two spaces before each path, bytewise sorted under `LC_ALL=C`, a final newline retained, and SHA-256 over the resulting byte string. An independent `sha256sum | sort | sha256sum` pipeline reproduced the in-memory result.

- `b5b5d673b64732ebd9efa85d3183a6e17c7ce167226eed1b15755aaf3e9cd0a9  constitution/MONKEYBEE_CONSTITUTION_v8.md`
- `6d3a9ddb68f2f9c3f8e3a169a9b44cd9513a91a60ad3e848e0b0d3357a88c877  constitution/SPECCARD_PIPELINE.md`
- `09cf724620fb8173640ed542a1ed18277e09eafb8be62b0302c93a9ec8f47739  shell/MONKEYBEE_SHELL_CORPUS_v8.md`
- `62e72bae29fe1e258f6b97a3719dbad7123307d1ef92e893749cece8c5005d77  reports/FIX_APPLICATION.md`
- sorted-manifest SHA-256: `dd5266aa80bd0ecdef7780fa670689427e70faee63d279efa1561e0c4e24d8e9`.

### Post-report diagnostic failures and corrections

The earlier R14 failures are recorded in `reports/FIX_APPLICATION.md` §15 and the preceding R14 checkpoints. The following failures occurred while validating the final report mutation; none was credited as evidence:

1. The first Section-13/14 bounded rerun parsed the table-header digit in “Rev 7” as a schema-source line and falsely reported `79` schema entries / `143` total. Restricting the parser to B.1–B.5 data rows reproduced `78` / `142` and exact set equality.
2. The first final registry validator reused Perl capture `$1` after its marker-count loop had overwritten the row-ID match. It emitted uninitialized-value warnings and stopped at sequence row 1. Saving the row ID before marker counting reproduced `43`, `001–043`, and `129/129`.
3. The first combined normative/targeted/clean-room wrapper failed JavaScript parsing before any nested check launched because a literal Markdown backtick around `usecmap` terminated the command string. The byte escape replacement allowed the checks to run.
4. The next normative and targeted invocations decoded files to Unicode but omitted UTF-8 source semantics for their non-ASCII command literals, causing false §34.9-binding and A02 failures. In the same round, the clean-room scanner failed compilation because its strict counters were undeclared. UTF-8 source mode and explicit counter scope corrected those diagnostics.
5. The corrected normative validator then used the wrong local heading text, `## 34. Refinement and freeze protocol`; the artifact's stable heading is `## 34. Plan refinement and freeze protocol`. The exact local heading produced the final pass.
6. The corrected added-line scanner excluded a four-plus prefix rather than Git's three-plus file header, so it counted three `+++` headers and returned `117` instead of `114`. The three-plus exclusion reproduced `114` with zero claim, prohibited-name, or URL hits.
7. The first final format wrapper omitted the opening delimiter of its frontmatter regex and failed Perl compilation. The corrected bounded regex produced the UTF-8/provenance/fence/whitespace/conflict-marker pass above.

No artifact premise was inferred from any diagnostic failure. The two source-line extraction regression remains the only substantive SELF-CHECK failure; it triggered the persisted return to `REVISE`, loss-preserving repair, and full-suite restart.

### Post-submission shared-state observation — 2026-07-14T22:23:41+02:00

After the submission transition and first checkpoint-hash read, `HEAD` and `origin/main` advanced concurrently again, from `4aa7e83d84fd5b1b92925da49b9aa8c069ef7b10` to `77c694e6ec71c0963f680affab645ae91c3956b9`. G1 performed no commit or push. A read-only baseline-to-current-HEAD comparison still reports the four G1 artifact blobs byte-identical to admitted baseline `353d72e0d52b831415af7035d593b85991560760` before the working changes, so the frozen component hashes and manifest remain unchanged.

The latest status contains only the five exclusive G1 modifications and no untracked file; the five unrelated paths seen at the immediately preceding status read are no longer dirty. G1 did not inspect their new bodies or infer whether they were committed, reconciled, or otherwise changed. This append-only observation supersedes only the live status/HEAD snapshot, not the historical earlier observations or any R14 evidence boundary.

## Root handoff checkpoint-navigation correction — 2026-07-14T22:30:48+02:00

- root finding: one checkpoint-navigation defect, not an artifact defect; the preserved R13 block's bare `Current checkpoint` heading could be mistaken for live state;
- repair: renamed that heading `Historical R13 checkpoint — superseded by the R14 submission` and added a concise note scoping its R14-unassigned and terminal-transition statements to R13 while naming R15 as the next round;
- navigation result: live state surfaces `1`; unqualified `Current checkpoint` headings `0`; historical R13 surfaces `1`; historical R14-unassigned statements `4/4` qualified; phase/next-round conflicts `0`;
- integrity result: UTF-8 `PASS`; fences balanced; trailing whitespace `0`; conflict markers `0`; state-only `git diff --check` exit `0`;
- frozen-artifact result: all four component SHA-256 values remain those recorded above and sorted manifest remains `dd5266aa80bd0ecdef7780fa670689427e70faee63d279efa1561e0c4e24d8e9`;
- diagnostic disclosure: the first navigation validator expected three R14-unassigned matches and failed on four. The bounded match read showed one timestamped R13 transition row plus three statements under the explicit R13 scope; the corrected four-location check passed. The failed count was not credited as evidence and required no content repair or FSM rollback;
- terminal route: `SUBMIT-FOR-REVIEW`; next round `R15`; G1 stops idle without spawning or routing a reviewer.

## R13 intake — persisted before artifact edits

- intake boundary: R13 sole lens `identity-law consistency`; `FILED`, mechanically `VALIDATED`, reviewer `TERMINATED`; counts `A=3`, `B=2`, `C=0`; marginal-only `NO`.
- filed-time correction: the filed start time was corrected append-only to after `2026-07-14T20:12:46+02:00` and at or before `2026-07-14T20:18:44+02:00`.
- validation caveat: a bounded `AUDIT_FINDINGS_LEDGER.md` exposure carries residual priming risk. Neither that exposure nor the filed-time correction validates, regrades, or otherwise changes a finding.
- bound input manifest:
  - `aed9cd3863460715ad8bfcf39fe7bd846e6ce8618d22572715eebf7b28a04179  constitution/MONKEYBEE_CONSTITUTION_v8.md`
  - `ceac345d4d580efc6edee81fb7052fc0ffd3ebc97df97b3c24e271ddf8a6a8b3  constitution/SPECCARD_PIPELINE.md`
  - `281fbd34c1a777a6f5158a6b6dfaeab56306cb75131013d3a1701f5b6f3468e7  shell/MONKEYBEE_SHELL_CORPUS_v8.md`
  - `845d8f5645cb7810596d287ea2598e8fd1a90f07b4483c1e22f06964413ef695  reports/FIX_APPLICATION.md`
  - manifest SHA-256: `9e81eaaa03c1368335b8aaff260af0ff918d7a6a76ca45bed85a00d4313743ec`
  - G1 state pre-task SHA-256: `8b52bce65f71d2e470b124ab1fb921a9902f12a49f4abdb61e81ee480aad18fc`
- exact measured process attestation supplied by the root orchestrator in the task packet: session `monkeybee-pdf-mass-context-repo--g1r`, pane `%114`, PID/pane PID `3732669`; process `gpt-5.6-sol`, reasoning effort `ultra`; live TUI Service tier `priority`, footer `gpt-5.6-sol ultra fast`. The human required the model family and fast mode but did not personally supply the PID/pane observation. This task-packet attestation is not campaign evidence derived from `.ntm/`.
- packet handling law: reviewer grades are metadata, not proof. Each premise below must succeed independently against the local authority chain before repair. Grade B remains Grade B even if its premise succeeds. A conscious non-repair must cite the local premise failure. `R14` remains unassigned.

| Finding | Filed grade | Exact routed premise | Exact loss guard | Intake disposition |
|---|---|---|---|---|
| `R13-A01` | `A` | Semantic-report and run-observation identities allegedly lack a closed typed grammar. | Preserve semantic-body/run-observation separation, telemetry exclusion from D0 identity, determinism limits, and D1/D2 ratification. | `PENDING-LOCAL-PREMISE-TEST`; no artifact change authorized by the grade alone. |
| `R13-A02` | `A` | Generated Appendix B families and validator allegedly omit the common section 10.8 report envelope. | Preserve every B.1-B.5 family field, B.2 telemetry exclusion, non-normative examples, and pending-ratification markers. | `PENDING-LOCAL-PREMISE-TEST`; no artifact change authorized by the grade alone. |
| `R13-A03` | `A` | `DivergenceReport` B.4 allegedly omits the section 10.6 mandatory processor version. | Preserve artifact ID, configuration, environment, profiles, input/view/derivation identities, and witness evidence as separate facts. | `PENDING-LOCAL-PREMISE-TEST`; no artifact change authorized by the grade alone. |
| `R13-B01` | `B` | Divergence outputs allegedly are not canonically bound to processors; unstated positional pairing is the unresolved premise. | Preserve comparison symmetry, per-processor artifact/configuration/environment facts, and independent output identities. | `PENDING-LOCAL-PREMISE-TEST`; the grade may not be promoted. |
| `R13-B02` | `B` | `StableSnapshot` allegedly leaves authority-version identity guarantees implicit; out-of-band `AuthorityId` guarantees are the unresolved premise. | Preserve all three source identity classes, `len: None` partial snapshots, range-scoped evidence, and source-change revocation. | `PENDING-LOCAL-PREMISE-TEST`; the grade may not be promoted. |

### Replacement-owner read diagnostics before premise testing

- Initial whole-artifact reads of the Constitution and shell exceeded display bounds. They were corrected with contiguous bounded reads covering every line of each artifact; no missing interval was substituted or reconstructed.
- The first bounded continuation of Rev 7 §10 was interrupted by context compaction. `AGENTS.md` was immediately reread in full, then §§10.6-10.10 and §11 were completed from bounded local ranges.
- A combined read of Rev 7 §§33-34 and Appendix B returned a truncated aggregate display. The §34 interval was reread as four contiguous bounded ranges; §§33 and Appendix B were already present across their complete bounded subranges. This was a display failure, not an authority or content discrepancy.
- The human then explicitly requested another full `AGENTS.md` reread; it was completed before continuation.
- A combined targeted read of Constitution report loci, shell Appendix B, and the fix map also exceeded the aggregate display bound. Constitution §§10.6 and 10.8-10.10, both contiguous halves of shell Appendix B, and the relevant fix-map/self-check regions were then reread in smaller bounded calls. No premise relies on omitted display text.
- Process-provenance correction before `SELF-CHECK`: the intake originally said “exact process attestation supplied by the human” and “This is a human-supplied process attestation.” Both provenance phrases were mistaken. The measured PID/pane observation came from the root orchestrator in the task packet; the human required the model family and fast mode. The original wording is preserved in this correction record rather than silently erased. No attested value, premise, repair, or other semantic disposition changed.
- The first provenance-correction patch attempt matched the diagnostic bullets in the wrong order and failed verification without changing a file. Exact local anchors were then read and this provenance-only correction was reapplied successfully.
- No web/search, prohibited source or documentation, external action, SpecCard semantic body, measurement, comparison execution, Bead, pseudo-Bead, code, scaffold, branch, worktree, stash, commit, reviewer spawn, or reviewer route occurred.

## R13 independent premise test — 2026-07-14T20:44:46+02:00

The replacement owner tested each allegation against the hierarchy rather than adopting the reviewer rationale. Constitution v8 is the current product-law home; the locally cited Rev 7 §§6, 9-11, 22.4, 33-34, and Appendix B were used only where the Constitution had not superseded or closed the point. The searches were bounded to the four G1 artifacts and those named local canon regions. No external or prohibited source was contacted.

| Finding | Local evidence and premise result | Smallest qualifying disposition | Grade handling |
|---|---|---|---|
| `R13-A01` | **QUALIFIES.** Constitution §9.2 lists typed identity classes but has no `SemanticReportProtocolId`, `SemanticReportId`, `RunObservationProtocolId`, or `RunObservationId`. Constitution §10.8 names two parts and says packages may bind them “by IDs,” yet states no typed value grammar, protocol identities, canonical commitment boundaries, or non-cyclic linkage direction. Rev 7 §10.8 contains the same open prose. Absence search: Constitution §§9.2 and 10.8; vocabulary `SemanticReportProtocolId`, `SemanticReportId`, `RunObservationProtocolId`, `RunObservationId`, semantic-body backlink, observation-schema identity. | Add the four typed identities at §9.2 and close their value-level commitment/linkage law at §10.8. The run envelope binds the semantic ID one-way; the semantic body never absorbs telemetry or a run ID. Leave the octet grammar/hash/digest width unavailable until §9.2.1 D1/D2 ratification. | Remains filed Grade `A`; grade supplied no proof. |
| `R13-A02` | **QUALIFIES.** Constitution §10.10 validates only report-family fields and does not require §10.8's common pair. Shell Appendix B's introduction/table cite only §10.6; B.1, B.3, B.4, and B.5 omit both identities/envelope, while B.2 has an untyped semantic alias and run ID plus telemetry but no capability-identity set or separately versioned observation schema. | Extend §10.10's generator/validator law to the §10.8 common fields; update the shell Appendix B citation/table and all B.1-B.5 non-normative examples. Preserve every existing family field, B.2 telemetry/exclusion fact, and pending-ratification markers. | Remains filed Grade `A`; grade supplied no proof. |
| `R13-A03` | **QUALIFIES.** Constitution §10.6 `DivergenceReport` expressly requires processor versions. Shell B.4 processor records contain ID, executable/service artifact, configuration, and environment, but no separate version fact. | Add a separate `version` to each B.4 processor record and make the validator/fix map trace it. Do not collapse artifact, version, configuration, environment, profile, input/view/derivation, output, or witness facts. | Remains filed Grade `A`; grade supplied no proof. |
| `R13-B01` | **QUALIFIES.** Constitution §10.6 requires processor facts and external output identities but states no processor-to-output binding. Shell B.4 places processor records and output IDs in separate arrays and states no positional law. No local law makes their shared positions authoritative; relying on them would add unstated meaning. | At the §10.6 normative home, require an unordered identity-keyed processor-result set whose record binds the independently identified output to that processor's artifact/version/configuration/environment. Retain the external-output identity summary only as an exact generated projection of those bindings; array order carries no role or comparison result. Echo this in B.4 and its validator. | Remains filed Grade `B`; explicitly not promoted. |
| `R13-B02` | **QUALIFIES IN NARROWED FORM.** The filed uncertainty about whether guarantees are out-of-band is locally resolved: §9.1.1 already says a `StableSnapshot` depends on its authority contract. The remaining premise succeeds because neither §9.1.1 nor §9.2 defines what `AuthorityId` commits to, how `version_digest` is authenticated/domain-separated under that contract, or the non-reuse rule needed to distinguish byte states. Immutability, `len: None` limits, range evidence, and source-change revocation are already explicit and must not be rewritten away. | Clarify the existing out-of-band contract at §9.1.1 and add `AuthorityId` to §9.2: the authority ID binds a versioned authentication/version-token contract; the `(AuthorityId, version_digest)` tuple names the snapshot; distinct byte states cannot reuse the tuple; failure of those guarantees downgrades to `Ephemeral`; collision quarantine and §9.2.1 ratification still govern. Do not add a fourth source class or force `len: Some`. | Remains filed Grade `B`; explicitly not promoted. |

- premise failures requiring conscious non-repair: none. `R13-B02`'s broader suggestion was narrowed at its locally failed sub-premise rather than adopted wholesale.
- selected repair files: `constitution/MONKEYBEE_CONSTITUTION_v8.md`, `shell/MONKEYBEE_SHELL_CORPUS_v8.md`, and `reports/FIX_APPLICATION.md`. `constitution/SPECCARD_PIPELINE.md` requires no repair and must remain byte-identical.
- normative homes: source identity/typed identities remain in Constitution §9; report families and common identity envelope remain in Constitution §10; shell Appendix B remains a non-normative generated echo; repair traceability remains in `reports/FIX_APPLICATION.md`.
- loss-guard admission: all five routed loss guards remain mandatory. No concrete identity encoding, hash, crate atlas, SpecCard semantic content, comparison outcome, ratified D1/D2/D3 choice, or strengthened claim is admitted by these repairs.

## R13 SELF-CHECK evidence and final manifest

All checks below were run locally and read-only after the qualifying repairs. A passing validator is evidence only for its named relation; it does not ratify D1/D2/D3, validate the reviewer grades, or strengthen a campaign claim.

| Check | Reproduced result |
|---|---|
| Heading/feature preservation | Rev 7 has `554` unique level-2–4 headings; missing from the Constitution/shell union: `0`. The source has `5,266` unique nonblank lines; exact-line exceptions are `142`, comprising the previously accounted `139` plus exactly three R13 replacements: two `DivergenceReport` bullets and the prior single-paragraph §10.8 statement. The replacement law preserves those facts while closing the filed gaps. |
| SpecCard boundary | Registry rows `40`; exact sequence `MB-SC-R0-001`–`040`; rows with wrong marker count `0`; semantic bodies authored `0`. `constitution/SPECCARD_PIPELINE.md` remains byte-identical at `ceac345d4d580efc6edee81fb7052fc0ffd3ebc97df97b3c24e271ddf8a6a8b3`. |
| Report-family preservation | `jq` parsed exactly five Appendix B JSON values. Full family assertions pass for B.1–B.5, including every prior family field, each B.3 role/root/view/expected-state binding, all B.5 checker/availability/coverage fields, and B.2 wall time `22.1`, peak bytes `18432122`, and telemetry exclusion. |
| `R13-A01` regression | All four typed names occur in Constitution §§9–10: `SemanticReportProtocolId=3`, `SemanticReportId=5`, `RunObservationProtocolId=3`, `RunObservationId=5`. The one-way backlink, no-cycle rule, D0 telemetry exclusion, determinism bounds, and no-production-ID-before-D1/D2-ratification sentence are present. |
| `R13-A02` regression | Appendix B contains five each of `semantic_report_protocol_id`, `capability_identities`, `run_observation_protocol_id`, `run_observation_id`, and `excluded_from_semantic_identity`, plus ten `semantic_report_id` fields covering five bodies and five exact backlinks. Friendly `semantic_report_identity` alias hits: `0`. Each observation uses the separately versioned `org.monkeybee.run-observation.v8-example` schema. |
| `R13-A03` regression | B.4 contains two separate processor `version` facts and retains both artifact, configuration, environment, profile, proper-domain identity, witness, and independent output facts. |
| `R13-B01` regression | B.4 contains two in-record `output_artifact_id` bindings; processor IDs are unique; the order-independent set of bound outputs exactly equals `external_output_artifact_ids`. Constitution §10.6 states that array position carries no processor role, priority, outcome, or majority meaning. Grade remains `B`. |
| `R13-B02` regression | The `SourceIdentity` enum still has exactly three variants: one each of `VerifiedContent`, `StableSnapshot`, and `Ephemeral`; `StableSnapshot` still has `len: Option<u64>`. The out-of-band contract, tuple non-reuse/authentication, `len: None`/partial-range limits, range evidence, whole-source promotion law, source-change revocation, collision quarantine, and D1/D2 gate are present. Grade remains `B`; the locally failed broader sub-premise stays narrowed. |
| Fix-map traceability and homes | `R13-A01`, `A02`, `A03`, `B01`, and `B02` each occur exactly once in the R13 fix map. All `17` `OPEN-C0` ledger rows retain a mapping. The five affected normative headings occur exactly once and only in the Constitution; shell Appendix B cites §§10.6, 10.8, and 10.10 as a non-normative `generated-echo`. |
| Canonical inputs | All `9/9` SHA-256 values in `ledger/CANONICAL_HASHES.md` match: Rev 7 `df7e21d6ce280979eeb4848866ea975c7c934eebff50801191bf1d1c4c45de1b`; Charter `56bb8e26c47cc72805450ff4963b239572cecce7d678663b7d3fa672379cb9ca`; Charter Reasoning `51152bb22596a25dee7de93e679e0567d4ea58362d5bf4e97d8fb9583b76e7c6`; Work Order `03a97020fa38ae77f3bedd378bfb75e7a3eb83e6ec0be112d20d2cd7945d2dbf`; Audit Ledger `d4574ffb5c01ebc5097f65df296752466b00f7986d9094f7b53817a2ec7e5294`; pre-definition `6120ae863bbe72fcc94e8274659b6d3fe028680073a2b8466afca13043ad6bb4`; post-definition `a25d6943e8a9d9198c48bbbbaf631f374c985601972f179f032f305b587d6472`; re-audit `723036c527850652ad42f3ce48cfbdc5777b784a3099b03f640b9e8decb5b6e1`; overnight goal `84d6bfb04d5405933037e2e2718e4b46e556689728c17c045c8d9f2dd0bc8937`. |
| Clean room and phase | The R13 added-line diff has `0` hits for the prohibited processor-name vocabulary enumerated in `AGENTS.md` and `0` URL hits. Existing canon-derived prohibited-name law/scoped-claim occurrences were not altered into evidence. No source/documentation contact, fetch, web/search, measurement, comparison run, SpecCard body, code, scaffold, Bead/pseudo-Bead, external action, branch, worktree, stash, commit, reviewer spawn, or reviewer route occurred. |
| Claim vocabulary | Constitution/shell remain exactly `160` matching lines / `202` occurrences: `best=24`, `complete=113`, `fastest=0`, `mogged=1`, `supremacy=64`. The prior manual scoped dispositions remain unchanged; the R13 repair added no hit. |
| Formatting and scope | `git diff --check` passes; fences are balanced; all five owned Markdown files are UTF-8; trailing-whitespace and conflict-marker hits are `0`. Scoped worktree status shows only the Constitution, shell, fix report, and this state file modified; the SpecCard pipeline has no diff. |

### Diagnostic failures and corrections during R13

- Whole-artifact and several aggregate displays exceeded output bounds; each was corrected by contiguous bounded rereads. One context compaction interrupted Rev 7 §10; `AGENTS.md` was reread immediately and the section was resumed from disk.
- The first provenance-correction patch used the wrong diagnostic-bullet order and failed verification without changing a file. Exact anchors were read, the root-orchestrator provenance was applied, and the mistaken human-attribution phrases remain quoted in the correction record above.
- The first multi-check wrapper failed JavaScript parsing before any shell diagnostic executed. The checks were rerun as smaller commands and passed.
- One aggregate final-diff display truncated. `git diff --check` passed, and the Constitution, shell, and fix report diffs were then inspected separately; the durable state content was checked through the targeted state reads and this final patch. No diagnostic failure was treated as a content pass.

### Exact final four-file manifest

Method: ordinary `sha256sum` lines, bytewise sorted under `LC_ALL=C`, including the final newline, then hashed with SHA-256. The method reproduced the supplied pre-task manifest `9e81eaaa03c1368335b8aaff260af0ff918d7a6a76ca45bed85a00d4313743ec` before use.

```text
6c68a7881be552a74d0105c8e9e46b87c57b64c64a69e69eda7e215f628fcff6  constitution/MONKEYBEE_CONSTITUTION_v8.md
ceac345d4d580efc6edee81fb7052fc0ffd3ebc97df97b3c24e271ddf8a6a8b3  constitution/SPECCARD_PIPELINE.md
2ddcfb8d6267c3f2e4b3cff62423aa3eb440f719bca428151cb130a34420d245  shell/MONKEYBEE_SHELL_CORPUS_v8.md
70dc3710e607897f1d86841d469bf57c97302a298f3c5acfaa67894db317be1d  reports/FIX_APPLICATION.md
```

- manifest SHA-256: `dd86aaf319ecc87d0526d90b56b1993e8023925eeec4467ffd7807f89f92efb4`
- artifact status: all remain `PROPOSED`; D1/D2/D3 remain awaiting human ratification; Appendix B remains non-normative; SpecCard semantic bodies remain unavailable.
- terminal disposition: stop at `SUBMIT-FOR-REVIEW`. `R14` is unassigned.

## Historical R13 checkpoint — superseded by the R14 submission

**Historical navigation note:** The R14-unassigned and terminal-transition statements in this preserved section were true at the R13 checkpoint only. Live owner state is governed by the R14 submission and transition record above; the next round is R15. Nothing in this historical block reassigns the current phase or next round.

- branch: `main`, tracking `origin/main`
- files read in full: `AGENTS.md`, `INDEX.md`, `MONKEYBEE_CAMPAIGN_CHARTER_v1.md`, `OVERNIGHT_GOAL.md`, `CYCLE_0_WORK_ORDER.md`, `AUDIT_FINDINGS_LEDGER.md`, and `ledger/prompts/OWNER_MARCHING_ORDERS.md`
- active goal: execute the document-space portion of Cycle 0 and stop this owner turn at `SUBMIT-FOR-REVIEW`
- authority chain: human instructions; `MONKEYBEE_CAMPAIGN_CHARTER_v1.md`; `CYCLE_0_WORK_ORDER.md` §§1–3; `AUDIT_FINDINGS_LEDGER.md`; then the named Rev 7 stable sections
- writable paths: `constitution/**`, `shell/**`, `reports/FIX_APPLICATION.md`, and this checkpoint only
- partial outputs at the `DRAFT` checkpoint: `constitution/MONKEYBEE_CONSTITUTION_v8.md`, `constitution/SPECCARD_PIPELINE.md`, `shell/MONKEYBEE_SHELL_CORPUS_v8.md`, `reports/FIX_APPLICATION.md`, and this state file
- decisions: preserve stable Rev 7 section identifiers; put each law in one normative home; keep generated restatements as citations or marked echoes; derive Appendix B from §10.6 obligations; create SpecCard registry, slots, extraction protocol, and review template without semantic bodies
- unavailable inputs: `ALIEN_ARTIFACT.md` for R2-N7 and `PROJECT_OVERVIEW.md` for R2-N8; neither may be reconstructed
- owner fetches: none
- additional files read in full or bounded task-relevant regions: `DISPUTES.md`; `reports/INGEST_REPORT.md`; Rev 7 §§4, 6, 8–11, 30.1.1, 33–35, Appendices A–B; and the exact loci for §3.0, §13.1/13.5/13.9, §16.5/16.8, §21.1/21.7.1, §22.3/22.4, §23.12, §24.2, §31.4, and §32
- structural source coverage: all 554 unique Rev 7 level-2–4 headings survive exactly in the Constitution/shell union. All 139 unique nonblank source lines without an exact output copy were manually accounted for as title/provenance restructuring, Rule 13 rewrites, OPEN-C0/cross-owner fixes, corrected sketches, or schema-derived Appendix B replacement; no unaccounted feature loss remains.
- proposed-decision dependency at the `DRAFT` transition: no `decisions/**` artifact existed then. D1–D7, `DECISION_INDEX.md`, and `DEFERRED_REGISTER.md` subsequently arrived and were reread in full before SELF-CHECK.
- cross-owner reconciliation: D1/D2 remain a coupled proposed choice; §9.2.1 selects no grammar and requires hash/grammar ratification before the first external commitment. D3 remains proposed; `DecodedContainerId` is the sole canonical name and `DecodedStreamId` appears only as the D-004 campaign-layer drift label. D4's checker binding remains proposed. D-005 controls over D5's proposal for G1 purposes: runtime dependency policy and cryptographic-crate selection remain pending human routing or ratification.
- Rule 13 gate: the human-reported early scan found 167 matching lines. G1's first post-extraction scan found 203 lines / 252 occurrences. After individual manual disposition and feature-preserving rewrites, the final pre-SELF-CHECK scan found 160 lines / 202 occurrences: Constitution 42 lines (`best` 9, `complete` 25, `fastest` 0, `mogged` 0, `supremacy` 16); shell 118 lines (`best` 15, `complete` 88, `fastest` 0, `mogged` 1, `supremacy` 48). Forty-three ordinary hit-bearing lines and 50 occurrences were rephrased. All retained uses are typed outcomes, scoped release/capability envelopes, explicit comparison-claim lifecycle/denominator/risk structures, foundry trial/baseline protocols, or Rule 13 prohibition examples; no promotional claim is asserted.
- SELF-CHECK result: nine canonical hashes match; every ledger ID is mapped; 17 `OPEN-C0` rows have 14 applied, one applied-proposed-binding, and two source-unavailable dispositions; 40 sequential SpecCard slots remain pending with zero semantic bodies; five Appendix B JSON examples parse and satisfy mandatory-field assertions; provenance, fences, whitespace, normative-home sentinels, §34.9 text, and owned-path/type checks pass.
- SELF-CHECK repairs: removed an accidental project-TLV/post-decode identity selection in favor of the D1/D2/D3 ratification gate; expanded R1-9 to all five proposed isolation/detection axes; corrected the report's 18-versus-17 `OPEN-C0` accounting and R1-10 status; made inherited §35 dependency/crypto defaults explicitly pending under D-005; and removed the last ordinary Rule 13 use outside the enforcement record.
- verification failures/fallbacks: local `ruby` was unavailable, so Appendix B validation passed with `/usr/bin/jq`; a first slot-sequence command used two-digit expected IDs and was rerun with three-digit IDs; a first §34.9 comparator excluded the EOF closing sentence and passed after its range was corrected. These were verifier false starts, not content failures.
- remaining unresolved inputs: R2-N7 (`ALIEN_ARTIFACT.md`) and R2-N8 (`PROJECT_OVERVIEW.md`) remain `SOURCE-UNAVAILABLE`; all G2 choices remain proposed until human ratification.
- submission result: the original suite and the bounded R13 successor suite pass. Current totals are 9/9 canonical hashes, 554/554 source headings, 142 accounted exact-line exceptions, 17 ledger mappings, 40 pending SpecCard slots with zero bodies, five validated JSON examples with common identity envelopes, five passing R13 regressions, and the unchanged 160-line Rule 13 disposition.
- next transition condition: none within this turn. The replacement owner stops at `SUBMIT-FOR-REVIEW`; `R14` remains unassigned and only a newly routed packet may authorize another `REVISE` transition.

## Root post-handoff accounting receipt — `2026-07-16T09:44:30+02:00`

`generated-by: root orchestrator`; `inputs: live human target reduction, pushed R62 qualification at 241e63f8465537ed4bc2e204e5814fb84700e208, gauntlet/ROUND_LOG.md, gauntlet/CONVERGENCE_REPORT.md, this checkpoint`; `status: PROPOSED`; `evidence-status: provisional-pending-substrate`.

- This receipt does not reopen or replace G1's persisted owner FSM, dispositions, artifacts, or terminal handoff. Pre-receipt checkpoint identity was 769 newline-terminated lines / 149,894 bytes / `00fbad864166c7acc4ad2219db155fb9714fced730ff8a06eb7a21a36ad34625`.
- The controlling later accounting is 22 accepted filings overall / 12 against the C1 delta plan, reached only after the transcript-qualified R62 filing. The human's later instruction was an approximate-20 accepted target, not a separate requirement to reach 22. Earlier G1 statements describing a reported or bounded 22/12 landing are chronology-bound control records; they neither forecast nor validate the later R62 chain.
- R17 and R18 remain unexecuted `UNREVIEWED-OMISSION` rows with no packet, lens application, finding, grade, pass state, coverage, or convergence credit. The later R62 filing does not substitute for either lens or change that disclosure requirement.
- G1's four-artifact manifest remains `24327520e55a7324f69f12244d7ea05a6d0f9997da68fd19c111659740c60292`; no G1 artifact byte changed in this post-handoff pass. Morning reporting must preserve the omissions, unresolved D1/D2/D3 and other ratification gates, source-unavailable boundaries, and Convergence `NOT ESTABLISHED`.
